- 검토 결과
	1. 높음 : 저수율 lot이 없으면 전체 분석이 즉시 실패합니다. status_node가 빈 목록을 처리하지 않고 lots[0]에 접근합니다. 정상 배치나 임계값 설정 변경만으로 IndexError가 발생합니다. graph/nodes.py:34
	2. 높음 : LLM이 정의되지 않은 도구를 호출하거나 인자를 잘못 만들면 그래프가 예외로 종료됩니다. TOOLS_BY_NAME[...] 직접 인덱싱과 .invoke()에 오류 경계가 없습니다. 실제 OpenAI 호환 모델에서는 도구명/스키마 이탈을 반드시 복구 가능한 ToolMessage로 변환해야 합니다. graph/nodes.py:87
	3. 높음 : finalize 승인 기준이 LLM의 자기 신고 confidence 뿐이고, 최대 루프에서는 confidence와 근거 품질과 무관하게 승인됩니다. 따라서 근거가 없는 원인도 "승인된 결론"으로 리포트될 수 있습니다. 적어도 공정 로그의 실제 스펙 이탈, 유사 사례 수 같은 결정론적 증거 조건을 게이트에 포함해야 합니다. graph/nodes.py:100
	4. 높음 : HTTP EDS 모드의 TLS 검증 기본값이 꺼져 있습니다. 운영자가 EDS_MODE="http"만 켜면 인증서 검증 없이 사내 데이터를 전송합니다. 기본값은 검증 활성화로 두고, 개발 환경에서만 명시적으로 우회하도록 바꾸는 편이 안전합니다. config.py:19
	5. 중간 : HTTP EDS 구현은 인터페이스 계약을 지키지 않습니다. 로컬 구현은 자기 자신 제외 및 EDS_MIN_SIMILARITY 필터링을 하지만 HTTP 응답은 그대로 반환합니다. 벡엔드별로 분석 결과가 달라지고, 자기 wafer가 다시 집계될 수 있습니다. tools/eds_search.py:67
	6. 중간 : 도구 호출 하나에 finalize와 다른 도구가 함께 포함되면, 승인 후에도 뒤의 도구가 실행됩니다. 최종화가 실행 단위의 종료 의미라면 호출을 사전 검증하거나 승인 즉시 반복을 중단해야 합니다. graph/nodes.py:78
	7. 테스트 공백 : 테스트 28개는 모두 통과했지만, 빈 저수율 목록, 알 수 없는 도구명, 도구 실행 실패, HTTP EDS 응답 검증, 최대 루프 강제 종료 시 미확정 리포트 같은 실패 경로는 검증하지 않습니다. 현재 E2E도 고정된 mock 시나리오만 통과합니다.