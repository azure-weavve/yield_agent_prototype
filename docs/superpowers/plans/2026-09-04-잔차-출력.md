# 잔차 출력 (B) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 판별선(0.5)을 못 넘었지만 아랫선(0.25)은 넘은 1단 후보를 "잔차" 로 리포트에
싣고, 잔차만 있는 상태에서 게이트가 `weak_signal` 로 물러설 수 있게 한다.

**Architecture:** `Bundle.passing()` 은 그대로 두고 `Bundle.residuals()` 를 새 관문으로
연다(넓히면 지목·승인 경로로 샌다). 게이트에 `(2a) weak_signal` 을 `(2) no_signal` **앞**에
넣고, 잔차도 통과 근거와 **같은** 접기·순위 함수를 타 `final_claims` 에 실린다. 리포트는
`passes` 로 갈라 `[근거]` 대신 `[잔차]` 로 찍는다.

**Tech Stack:** Python 3.11, pytest, LangGraph. 콘솔이 cp949 라 테스트 실행 시
`PYTHONIOENCODING=utf-8` 을 붙인다.

**Spec:** `docs/superpowers/specs/2026-09-04-잔차-출력-design.md`

## Global Constraints

- 브랜치 `feat/residual-output`, 기준선 **main `250c0ac`, 472 passed**.
- 주석·문서·커밋 메시지는 **한국어**. 콘솔 출력에 em-dash 금지(cp949).
- 커밋 메시지 말미에 두 줄을 붙인다:
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` / `Claude-Session: https://claude.ai/code/session_01JdKNRwj2wzJQvWDcR31Pqe`
- **`passing()` 의 정의를 바꾸지 않는다.** 잔차는 `residuals()` 로만 들어온다.
- **확정(confirmed) 경로의 출력은 바이트 동일해야 한다.**
- 새 노브 기본값: `RESIDUAL_MIN_SCORE = 0.25`.
- 판정 하나를 신설하면 **게이트 verdict · mock 결론문 · 운영 리포트 sys 프롬프트 ·
  분석 sys 프롬프트 네 곳을 반드시 같이** 넣는다(Task 2·4가 나눠 맡되 Task 4에서 전수 확인).

## File Structure

| 파일 | 책임 | 이번 변경 |
|---|---|---|
| `ya_config.py` | 조정 노브 | `RESIDUAL_MIN_SCORE` 추가 |
| `graph/evidence.py` | Claim 투영·접기·순위 | `Bundle.residuals()` 신설, `ranked_groups(claims=None)` 인자 개방 |
| `graph/nodes.py` | 게이트 판정·리포트 조립·프롬프트 | `(2a) weak_signal` 분기, `step_back` 개문 조건, `[잔차]` 라벨, 분석 sys 프롬프트 |
| `graph/state.py` | 상태 스키마 | `finalize_status` 주석에 `weak_signal` 추가 |
| `llm/client.py` | mock 결론문·운영 프롬프트 | `weak_signal` 분기 2곳 |
| `tests/test_evidence.py` | 번들 계약 | Task 1 |
| `tests/test_graph_nodes.py` | 게이트·리포트 | Task 2·3 |
| `tests/test_mock_llm.py` | 두 클라이언트 계약 | Task 4 |

---

## Task 1: `Bundle.residuals()` 와 순위 함수의 대상 개방

**Files:**
- Modify: `ya_config.py` (노브 블록 (2) 판별선 아래)
- Modify: `graph/evidence.py:277-320` (`passing` 아래, `ranked_groups` 시그니처)
- Test: `tests/test_evidence.py`

**Interfaces:**
- Produces: `Bundle.residuals() -> list[Claim]`,
  `Bundle.ranked_groups(claims: list[Claim] | None = None) -> list[ClaimGroup]`
- Consumes: 기존 `Bundle.claims`(미통과 후보도 담겨 있다) · `Bundle.statuses`(tool -> status)

**배경:** `Bundle.claims` 는 **이미 미통과 후보를 담고 있다**(`claims: dict[str, Claim]`
주석: "미통과 후보도 담는다"). 지금은 `passing()` 만이 그 dict 를 좁혀 내보내는 유일한
문이라, 판별선 아래가 통째로 사라진다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_evidence.py` 끝에 추가한다. 픽스처 헬퍼는 이 파일에 이미 있는 것을 쓰지 말고
(있는 헬퍼가 통과 후보 전용이다) 아래처럼 finding 을 직접 만든다.

```python
def _weak_finding(tool="hyp_eqp_ch_commonality", hid="eqp_ch_commonality",
                  status="ok", score=0.4, target_pass=4, key="ETCH9_B"):
    """판별선(0.5)을 못 넘은 1단 후보 하나를 담은 finding."""
    return {
        "loop": 2, "tool": tool, "args": {},
        "result": {"hypothesis_id": hid, "status": status,
                   "legend": [{"level": "chamber", "columns": ["eqp_id", "ch_id"]}],
                   "candidates": [
                       {"claim_id": f"{hid}:chamber:CC002000:{key}", "step_seq": "CC002000",
                        "key": key, "level": "chamber", "passes": False,
                        "reject_reason": f"분리 점수 {score} < 0.5", "score": score,
                        "target_pass": target_pass, "target_total": 4,
                        "control_pass": 3, "control_total": 5},
                   ]},
        "thought": "약한 후보",
    }


def test_a_weak_candidate_becomes_a_residual():
    """판별선 아래 후보가 번들 밖으로 나가는 유일한 문 - 없으면 통째로 소각된다."""
    b = evidence.build_bundle([_weak_finding()])
    assert [c.claim_id for c in b.residuals()] == \
        ["eqp_ch_commonality:chamber:CC002000:ETCH9_B"]
    assert b.passing() == []            # passing 의 정의는 그대로다


def test_a_residual_is_not_a_passing_claim():
    """`passing()` 을 넓히면 statistical_passing(지목 가능)과 ranked_groups(1등 층 =
    승인 조건) 두 경로로 새어 **잔차로 confirmed 가 나간다.** 정의가 갈린 것을 잠근다."""
    b = evidence.build_bundle([_weak_finding()])
    assert b.statistical_passing() == []
    assert b.ranked_groups() == []


def test_a_candidate_below_the_residual_floor_is_not_a_residual():
    """0.05 짜리를 '근거' 란에 찍으면 엔지니어가 그것으로 설비를 세운다."""
    b = evidence.build_bundle([_weak_finding(score=0.05)])
    assert b.residuals() == []


def test_a_candidate_with_too_few_target_wafers_is_not_a_residual():
    """타깃 1장짜리는 신호가 아니라 표본 문제다 (판별선의 두 번째 조건과 같은 값을 쓴다)."""
    b = evidence.build_bundle([_weak_finding(target_pass=1)])
    assert b.residuals() == []


def test_a_candidate_from_a_non_ok_run_is_not_a_residual():
    """`no_paired_stratum` 은 '약한 신호' 가 아니라 '볼 것이 없었다' 다 - 조치가 다르다.

    status 는 후보가 아니라 **그 실행**의 성질이라 `Bundle.statuses[tool]` 로 본다.
    `reject_reason` 문자열을 파싱하는 것은 claim_id 에서 금지한 것과 같은 짓이다.
    """
    b = evidence.build_bundle([_weak_finding(status="no_paired_stratum")])
    assert b.residuals() == []


def test_a_failing_sensor_is_not_a_residual():
    """센서는 자기 판별선(0.8)을 갖고 이미 근거 전용이다 - 미통과분까지 실으면
    '왜' 후보가 두 겹이 된다."""
    finding = {
        "loop": 3, "tool": "compare_sensor_distribution",
        "args": {"step_seq": "CC002000"},
        "result": {"kind": "sensor", "status": "ok", "candidates": [
            {"claim_id": "sensor:CC002000:TEMP_1", "sensor_name": "TEMP_1",
             "effect_size": 0.4, "passes": False, "reject_reason": "효과크기 0.4 < 0.8",
             "target_mean": 1.0, "control_mean": 1.0, "target_std": 0.1,
             "control_std": 0.1, "n_target": 12, "n_control": 40}]},
        "thought": "2단",
    }
    assert evidence.build_bundle([finding]).residuals() == []


def test_ranked_groups_can_rank_a_given_list():
    """잔차도 **같은** 접기·순위 함수를 탄다 - 여기서 규칙을 다시 구현하면 순위 규칙이
    두 곳에 살게 되고, 그것이 C(축 간 순위)가 없앤 결함이다."""
    b = evidence.build_bundle([_weak_finding()])
    groups = b.ranked_groups(b.residuals())
    assert [g.lead.claim_id for g in groups] == \
        ["eqp_ch_commonality:chamber:CC002000:ETCH9_B"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_evidence.py -q -k "residual or ranked_groups_can_rank" --tb=short`
Expected: FAIL — `AttributeError: 'Bundle' object has no attribute 'residuals'`

- [ ] **Step 3: 노브를 추가한다**

`ya_config.py` 의 `COMMONALITY_PASS_MIN_TARGET` 바로 아래에 넣는다.

```python
# 잔차 아랫선 - 판별선(COMMONALITY_PASS_MIN_SCORE)을 못 넘은 후보 중 이 값 이상만
# '약한 신호' 로 싣는다. 0.25 는 판별선의 절반이며 실데이터를 본 값이 아니다 -
# COMMONALITY_PASS_* · SENSOR_PASS_MIN_EFFECT 와 같은 성격의 조정 노브다.
RESIDUAL_MIN_SCORE = float(os.getenv("RESIDUAL_MIN_SCORE", "0.25"))
```

- [ ] **Step 4: `residuals()` 를 구현한다**

`graph/evidence.py` 는 지금 `ya_config` 를 import 하지 않는다 — 파일 맨 위 import 블록에
`import ya_config` 를 추가한다(`from dataclasses import ...` 위, 프로젝트 관례대로
표준 라이브러리 다음).

`statistical_passing()` 바로 아래에 넣는다.

```python
    def residuals(self) -> list[Claim]:
        """판별선 아래의 1단 후보 중 '약한 신호' 라고 부를 수 있는 것.

        `passing()` 을 넓히지 않고 문을 따로 내는 이유: `statistical_passing()`
        (지목 가능한 것)과 `ranked_groups()`(1등 층 = 게이트 승인 조건)가 둘 다
        `passing()` 을 타므로, 넓히는 순간 **잔차로 confirmed 가 나간다.**

        status 를 `statuses[c.tool]` 로 보는 이유: status 는 후보가 아니라 그 실행의
        성질이고, `reject_reason` 문자열을 파싱하는 것은 claim_id 에서 금지한 짓이다.
        """
        return [c for c in self.claims.values()
                if not c.passes
                # 센서는 자기 판별선(0.8)을 갖고 이미 근거 전용이다
                and c.kind != "sensor"
                # "볼 것이 없었다"(no_paired_stratum 등)는 약한 신호가 아니다
                and self.statuses.get(c.tool) == "ok"
                and c.target_pass >= ya_config.COMMONALITY_PASS_MIN_TARGET
                and c.score >= ya_config.RESIDUAL_MIN_SCORE]
```

- [ ] **Step 5: `ranked_groups` 의 대상을 연다**

시그니처와 루프 첫 줄만 바꾼다. 본문은 손대지 않는다.

```python
    def ranked_groups(self, claims: list[Claim] | None = None) -> list[ClaimGroup]:
```

docstring 끝(닫는 `"""` 앞)에 한 문단을 더한다.

```
        `claims` 를 주면 그 목록을 대신 줄 세운다 - 잔차 경로가 **같은 접기·순위
        규칙**을 타게 하려는 것이다. 기본값은 통과 후보(`passing()`)이며, 기본값을
        바꾸면 확정 경로가 조용히 달라진다.
```

루프 첫 줄:

```python
        for claim in (self.passing() if claims is None else claims):
```

- [ ] **Step 6: 통과를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_evidence.py -q --tb=short`
Expected: PASS (새 테스트 7건 포함)

- [ ] **Step 7: 전체 스위트**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short 2>&1 | tail -3`
Expected: 479 passed (472 + 7). **줄어들면 멈추고 무엇이 깨졌는지 보고한다.**

- [ ] **Step 8: 커밋**

```bash
git add ya_config.py graph/evidence.py tests/test_evidence.py
git commit -m "feat(evidence): 판별선 아래 후보가 잔차로 나가는 문을 낸다"
```

---

## Task 2: 게이트 `(2a) weak_signal`

**Files:**
- Modify: `graph/nodes.py` (`_finalize_gate` 의 (2) 블록 **앞**, `_no_candidate_action` 의 `step_back`)
- Modify: `graph/state.py:25` (`finalize_status` 주석)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: Task 1 의 `bundle.residuals()`, `bundle.ranked_groups(claims)`
- Produces: `finalize_status == "weak_signal"` 상태와 `update["final_claims"]`(잔차 포함)

**배경:** 지금 잔차만 있는 상태는 (2)(3)(3b) 어느 것도 안 열려 `MAX_LOOPS` 까지 왕복하다
`inconclusive` 로 끝난다. 물러설 안내(`step_back`)도 안 붙는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_graph_nodes.py` 에 추가한다. `EQP_CH_BELOW_LINE`(미통과 1단 후보, status ok)은
이 파일에 **이미 있다** — 그대로 쓴다. `SENSOR_FINDING`·`PPID_SILENT` 도 있다.

```python
def test_a_weak_only_state_ends_as_weak_signal():
    """약한 신호와 무신호가 같은 출력이던 것을 가른다.

    지금은 (2)(3)(3b) 어느 것도 안 열려 루프 한계까지 왕복하다 inconclusive 로 끝나고
    final_claims 는 빈 목록이다 - '봤고 후보도 났는데 약하다' 가 통째로 소각된다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "", "hypothesis": "h", "confidence": 0.3},
        loop=2, update=update, findings=[EQP_CH_BELOW_LINE])
    assert update["finalize_status"] == "weak_signal"
    assert update["finalize_accepted"] is True
    ids = [c["claim_id"] for c in update["final_claims"]]
    assert ids == ["eqp_ch_commonality:chamber:CC002000:ETCH9_B"]
    assert "잔차" in verdict


def test_weak_signal_wins_over_no_signal():
    """한 축은 침묵하고 다른 축은 약한 후보를 낸 상태에서 두 조건이 동시에 참이다.

    '봤고 후보도 났는데 약하다' 가 더 많은 정보를 담은 사실이므로 그쪽이 이겨야 한다.
    뒤에 두면 같은 상태가 no_signal 로 먼저 빠져나가 잔차가 또 소각된다.
    """
    update = {}
    nodes._finalize_gate({"claim_id": "", "hypothesis": "h", "confidence": 0.3},
                         loop=2, update=update,
                         findings=[EQP_CH_BELOW_LINE, PPID_SILENT])
    assert update["finalize_status"] == "weak_signal"


def test_weak_signal_does_not_drop_a_passing_sensor():
    """(2a)의 하한은 statistical_passing() 이라 **센서만 통과한 상태에서도 열린다.**

    그때 잔차만 실으면 판별선을 넘은 센서 근거가 리포트에서 사라진다 - 잔차는 근거를
    밀어내는 것이 아니라 더하는 것이다.
    """
    update = {}
    nodes._finalize_gate({"claim_id": "", "hypothesis": "h", "confidence": 0.3},
                         loop=3, update=update,
                         findings=[EQP_CH_BELOW_LINE, SENSOR_FINDING])
    assert update["finalize_status"] == "weak_signal"
    ids = {c["claim_id"] for c in update["final_claims"]}
    assert "sensor:CC002000:TEMP_1" in ids
    assert "eqp_ch_commonality:chamber:CC002000:ETCH9_B" in ids


def test_a_submitted_claim_id_does_not_open_weak_signal():
    """지목을 제출한 것은 물러선 것이 아니다 - (2)(3)과 같은 하한이다.

    없으면 '확신도 0.9 로 없는 근거를 지목한' 제출이 곧바로 종료로 빠져나가
    환각이 물러섬으로 둔갑한다.
    """
    update = {}
    nodes._finalize_gate(
        {"claim_id": "지어낸:claim:id", "hypothesis": "h", "confidence": 0.9},
        loop=2, update=update, findings=[EQP_CH_BELOW_LINE])
    assert update.get("finalize_status") != "weak_signal"


def test_a_weak_only_state_is_told_it_can_step_back():
    """잔차가 있으면 물러설 길이 실제로 열려 있으므로 안내해야 한다.

    안 붙이면 LLM 은 종료할 길을 못 찾아 MAX_LOOPS 까지 왕복한다 - 예산 낭비 경로다.
    """
    verdict = nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:ETCH9_B",
         "hypothesis": "h", "confidence": 0.9},
        loop=2, update={}, findings=[EQP_CH_BELOW_LINE])
    assert "claim_id 를 비우고" in verdict
```

- [ ] **Step 2: 실패를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_graph_nodes.py -q -k "weak" --tb=short`
Expected: FAIL (5건). `finalize_status` 가 없거나 `inconclusive` 다.

- [ ] **Step 3: (2a) 분기를 (2) 앞에 넣는다**

`graph/nodes.py` 에서 주석 `# (2) 신호 없음 - 돌린 축에서 통과 후보가 하나도 없다.` 로
시작하는 블록 **바로 앞**에 삽입한다.

```python
    # (2a) 약한 신호 - 판별선은 못 넘었지만 아랫선을 넘은 후보가 있다.
    #      "봤고 후보도 났는데 이 표본으로는 확정할 만큼 갈리지 않았다" 는 (2)의
    #      "대조한 축에서 갈리는 것이 없었다" 와 **다른 사실**이고 조치도 다르다 -
    #      잔차는 표본을 늘리거나 대조군을 바꾸면 갈릴 수 있다. 그래서 no_signal 로
    #      뭉개지 않고 이름을 따로 준다.
    #
    #      **(2)보다 앞이다.** 한 축은 침묵하고 다른 축은 ok + 약한 후보를 낸 상태에서
    #      두 조건이 동시에 참인데, 정보가 더 많은 쪽이 이겨야 한다. 뒤에 두면 그 상태가
    #      no_signal 로 먼저 빠져나가 잔차가 또 소각된다.
    residuals = bundle.residuals()
    if (not bundle.statistical_passing() and not claim_id and residuals):
        update["finalize_accepted"] = True
        update["finalize_status"] = "weak_signal"
        update["final_hypothesis"] = hypothesis
        update["final_confidence"] = conf
        update["coverage"] = coverage
        # **통과 근거를 밀어내지 않는다.** 이 분기의 하한은 statistical_passing()
        # 이라 센서만 통과한 상태에서도 열린다 - 잔차만 실으면 판별선을 넘은 센서
        # 근거가 리포트에서 사라진다. picked 는 넘기지 않는다(`not claim_id` 가
        # 하한이므로 지목이 없다는 것이 이 분기의 전제다).
        _record_evidence(update, bundle.ranked_groups(bundle.passing() + residuals), None)
        return (f"약한 신호 ({_coverage_phrase(coverage)}): 판별선을 넘은 원인 후보는 "
                f"없고, 아랫선을 넘은 잔차 {len(residuals)}건을 근거로 싣는다. "
                f"확정이 아니라 '이 표본으로는 갈리지 않았다' 는 뜻이다. "
                f"리포팅으로 진행한다.")
```

- [ ] **Step 4: 물러설 안내를 연다**

`_no_candidate_action` 안의 `step_back` 계산에 잔차를 더한다.

```python
    opens_no_signal = "no_signal" in ran_statuses                       # (2)
    opens_weak = bool(bundle.residuals())                                # (2a)
```

그리고 `step_back` 조건에 `opens_weak` 를 더한다.

```python
    step_back = (" 지목할 것이 없어 물러설 때는 claim_id 를 비우고 finalize 하라."
                 if opens_no_signal or opens_weak or opens_no_data or opens_tool_failure
                 else "")
```

- [ ] **Step 5: 상태 스키마 주석을 고친다**

`graph/state.py` 의 `finalize_status` 주석 값 목록에 `weak_signal` 을 `no_signal` 다음에
넣는다.

```python
    finalize_status: str    # confirmed | weak_signal | no_signal | no_comparable_data | tool_failure | inconclusive | no_anomaly | unknown_target | isolated | control_insufficient | eds_lookup_failed | llm_call_failed
```

- [ ] **Step 6: 통과를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_graph_nodes.py -q --tb=short 2>&1 | tail -5`
Expected: PASS. **깨지는 기존 테스트가 있으면 그 이름을 기록하고 멈춘다** — 확정 경로가
달라졌다는 뜻이라 (2a)의 하한을 잘못 쓴 것이다.

- [ ] **Step 7: 전체 스위트**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short 2>&1 | tail -3`
Expected: 484 passed (479 + 5).

- [ ] **Step 8: 커밋**

```bash
git add graph/nodes.py graph/state.py tests/test_graph_nodes.py
git commit -m "feat(gate): 잔차만 있는 상태를 weak_signal 로 끝낸다"
```

---

## Task 3: 리포트의 `[잔차]` 라벨

**Files:**
- Modify: `graph/nodes.py` (`report_node` 의 `for group in claims:` 루프)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: Task 2 가 실어 둔 `final_claims`(항목 대표에 `passes`·`reject_reason` 이 있다)

**배경:** `report_node` 는 지금 모든 항목을 `[근거 {rank}]` 로 찍는다. 잔차를 그대로
찍으면 **통과 근거와 글자 하나 다르지 않다** — 이 기능이 만드는 가장 큰 위험이다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

상태 dict 는 이 파일에 이미 있는 report_node 테스트(`[근거 1]` 을 단언하는 것, 1100줄
부근)를 **본떠서** 쓴다 - 키가 빠지면 report_node 가 KeyError 로 죽어, 라벨이 아니라
픽스처를 디버깅하게 된다.

```python
def test_report_labels_a_residual_as_residual_not_as_evidence():
    """라벨이 없으면 미통과 후보가 통과 근거와 글자 하나 다르지 않다.

    엔지니어는 그 줄을 보고 설비를 세운다. 왜 약한지(reject_reason)도 같은 줄에 남긴다 -
    수치만 있으면 '분리 점수 0.4' 가 강한 근거인지 약한 근거인지 읽을 수 없다.
    """
    state = {
        "final_claims": [{
            "claim_id": "eqp_ch_commonality:chamber:CC002000:ETCH9_B",
            "level": "chamber", "key": "ETCH9_B", "step_seq": "CC002000",
            "score": 0.4, "passes": False, "reject_reason": "분리 점수 0.4 < 0.5",
            "target_pass": 4, "target_total": 4, "control_pass": 3, "control_total": 5,
            "rank": 1, "kind": "statistical", "target_wafers": [], "control_wafers": [],
            "confounded_with": [], "rolled_up_as": [],
        }],
        "finalize_status": "weak_signal", "final_hypothesis": "h",
        "final_confidence": 0.3, "status_summary": "s", "findings": [],
        "target_wafers": ["W1"], "target_group": ["W1"], "messages": [],
    }
    report = nodes.report_node(state)["report"]
    assert "[잔차 1]" in report
    assert "[근거 1]" not in report
    assert "분리 점수 0.4 < 0.5" in report


def test_report_still_labels_a_passing_claim_as_evidence():
    """확정 경로의 라벨은 그대로다 - 잔차 분기를 넣다가 통과 근거까지 바꾸면 안 된다."""
    state = {
        "final_claims": [{
            "claim_id": "eqp_ch_commonality:chamber:CC002000:ETCH9_B",
            "level": "chamber", "key": "ETCH9_B", "step_seq": "CC002000",
            "score": 1.0, "passes": True, "reject_reason": None,
            "target_pass": 3, "target_total": 3, "control_pass": 0, "control_total": 3,
            "rank": 1, "kind": "statistical", "target_wafers": [], "control_wafers": [],
            "confounded_with": [], "rolled_up_as": [],
        }],
        "finalize_status": "confirmed", "final_hypothesis": "h",
        "final_confidence": 0.9, "status_summary": "s", "findings": [],
        "target_wafers": ["W1"], "target_group": ["W1"], "messages": [],
    }
    report = nodes.report_node(state)["report"]
    assert "[근거 1]" in report
    assert "[잔차" not in report
```

- [ ] **Step 2: 실패를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_graph_nodes.py -q -k "labels_a_residual or still_labels_a_passing" --tb=short`
Expected: 첫 테스트 FAIL(`[잔차 1]` 이 없다), 둘째는 이미 PASS.

- [ ] **Step 3: 라벨을 가른다**

`report_node` 의 루프를 아래로 바꾼다.

```python
    for group in claims:
        mark = " ←서술 기준" if group.get("picked_by_llm") else ""
        # **잔차는 다른 이름으로 찍는다.** 판별선을 못 넘은 후보를 `[근거]` 로 찍으면
        # 통과한 것과 글자 하나 다르지 않아, 엔지니어가 그 줄을 보고 설비를 세운다.
        # 기본값이 True 인 이유: 이 자리를 지나는 dict 는 전부 게이트가 만든 것이라
        # `passes` 가 늘 있지만, 없으면 '근거' 로 읽는 쪽이 옛 동작과 같다.
        label = "근거" if group.get("passes", True) else "잔차"
        # 번호는 위치가 아니라 **등수**다. 동점이 1·2 로 찍히면 앞선 것이 더 강해
        # 보이는데, 그 오독을 막으려고 등수를 따로 계산해 둔 것이다.
        report += (f"\n[{label} {group.get('rank', '?')}]{mark} "
                   f"{evidence.format_group_line(group)}")
        # 왜 약한지를 같은 줄에 남긴다 - 수치만 보면 0.4 가 강한지 약한지 못 읽는다.
        # 문구를 새로 짓지 않는다: `_passes` 가 만든 문장이 이미 정확하다.
        if not group.get("passes", True) and group.get("reject_reason"):
            report += f" (판별선 미달: {group['reject_reason']})"
        if group.get("more_below"):
            report += (f"\n[근거 ...] 순위 밖 {group['more_below']}건은 생략했다 "
                       f"(전체는 분석 과정 기록에 있다)")
```

- [ ] **Step 4: 통과를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_graph_nodes.py -q --tb=short 2>&1 | tail -3`
Expected: PASS

- [ ] **Step 5: 전체 스위트와 커밋**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short 2>&1 | tail -3`
Expected: 486 passed (484 + 2).

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "fix(report): 잔차를 근거와 같은 이름으로 찍지 않는다"
```

---

## Task 4: LLM 계약 세 곳

**Files:**
- Modify: `llm/client.py` (mock 결론문 status 분기, 운영 리포트 sys 프롬프트)
- Modify: `graph/nodes.py` (`ANALYZE_SYSTEM_PROMPT`)
- Test: `tests/test_mock_llm.py`

**Interfaces:**
- Consumes: Task 2 의 `finalize_status == "weak_signal"`

**배경:** 판정 하나를 신설하면 대응하는 자리가 정해져 있다(조사 §D). 하나라도 빠지면
그 경로에서만 계약이 사라진다 — 지난 순열 p 교정에서 리뷰 Important 2건이 그 구멍이었다.
분석 프롬프트가 특히 중요하다: 없으면 LLM 이 약한 후보를 계속 지목하고 게이트가 계속
반려해 왕복이 남는다(예산 낭비 경로를 반만 닫는 셈이다).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_mock_llm.py` 에 추가한다. `_openai_client()` 헬퍼는 이 파일에 이미 있다.

```python
def test_mock_report_has_a_sentence_for_weak_signal():
    """판정 어휘를 늘리면 mock 결론문도 같이 늘려야 한다 - 안 그러면 새 판정이
    `else` 로 떨어져 LLM 이 쓴 가설이 확정처럼 찍힌다."""
    from llm.client import ScriptedMockLLMClient
    report = ScriptedMockLLMClient().generate_report(
        target_wafers=["W1"], target_source="manual", target_group=["W1"],
        status_summary="s", findings=[], hypothesis="ETCH9_B 편중", confidence=0.3,
        finalize_status="weak_signal")
    assert "약한 신호" in report
    assert "판별선" in report


def test_operational_prompt_tells_the_report_what_weak_signal_means():
    """운영 리포트 LLM 은 판정 이름만 받는다 - 무엇인지 안 알려주면 잔차를 원인으로
    단정하거나, 반대로 신호 없음으로 뭉갠다. 다음 행동(표본·대조군)까지 적게 한다."""
    client = _openai_client()
    client.generate_report(
        target_wafers=["W1"], target_source="manual", target_group=["W1"],
        status_summary="s", findings=[], hypothesis="h", confidence=0.3,
        finalize_status="weak_signal", coverage=None, claims=[])
    assert "weak_signal" in client.llm.seen_sys
    assert "판별선" in client.llm.seen_sys
    assert "확정 결론을 쓰지 마라" in client.llm.seen_sys


def test_analyze_prompt_tells_the_llm_to_step_back_on_weak_candidates():
    """게이트가 받아 주지 않는 것을 계속 지목하게 두면 왕복만 남는다.

    반려 문구(`_gate_rejection`)에도 안내가 있지만, 이 저장소는 규칙을 판정과 프롬프트
    양쪽에 적는다 - 한쪽만 있으면 LLM 은 체크리스트를 소화하러 간다.
    """
    from graph import nodes
    assert "판별선을 넘지 못한 후보만" in nodes.ANALYZE_SYSTEM_PROMPT
    assert "잔차" in nodes.ANALYZE_SYSTEM_PROMPT
```

- [ ] **Step 2: 실패를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_mock_llm.py -q -k "weak" --tb=short`
Expected: FAIL (3건)

- [ ] **Step 3: mock 결론문에 분기를 넣는다**

`llm/client.py` 의 `elif finalize_status == "no_signal":` 블록 **앞**에 넣는다(읽는 순서를
게이트 판정 순서와 맞춘다).

```python
        elif finalize_status == "weak_signal":
            conclusion = ("약한 신호 - 후보는 나왔으나 판별선을 넘지 못했다. "
                          "원인 없음이 아니라 이 표본으로는 확정할 만큼 갈리지 않았다는 "
                          "뜻이며, 타깃/대조군을 넓히면 갈릴 수 있다. "
                          "아래 [잔차] 줄이 그 후보들이다.")
```

- [ ] **Step 4: 운영 리포트 sys 프롬프트에 규칙을 넣는다**

`llm/client.py` 의 `"판정이 no_signal 이면 ..."` 문장 **앞**에 넣는다.

```python
            "판정이 weak_signal 이면 '약한 신호'로 서술하라 - 후보는 나왔으나 "
            "판별선을 넘지 못한 것이니 원인으로 단정하지 말고, 무엇을 하면 갈리는지"
            "(타깃/대조군 표본을 넓히기)를 후속 조치로 적어라. 확정 결론을 쓰지 마라. "
```

- [ ] **Step 5: 분석 sys 프롬프트에 규칙을 넣는다**

`graph/nodes.py` 의 `ANALYZE_SYSTEM_PROMPT` 에서 2단 센서 항목 **다음** 줄에 넣는다.

```
- **판별선을 넘지 못한 후보만 있으면 지목하지 마라.** claim_id 를 비우고 낮은 확신도로 finalize 하면 게이트가 그 후보들을 잔차로 리포트에 싣는다 - 억지로 지목하면 반려되고, 같은 반려를 되풀이하면 루프 예산만 태운다.
```

- [ ] **Step 6: 통과를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_mock_llm.py -q --tb=short 2>&1 | tail -3`
Expected: PASS

- [ ] **Step 7: 네 계약이 전부 들어갔는지 눈으로 센다**

Run: `grep -rn "weak_signal\|잔차" graph/nodes.py llm/client.py graph/state.py | grep -v "^.*#"`
Expected: 게이트 verdict · 분석 프롬프트 · mock 결론문 · 운영 sys 프롬프트 · state 주석이
전부 나온다. **하나라도 없으면 그 자리가 빠진 것이다.**

- [ ] **Step 8: 전체 스위트와 커밋**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short 2>&1 | tail -3`
Expected: 489 passed (486 + 3).

```bash
git add llm/client.py graph/nodes.py tests/test_mock_llm.py
git commit -m "docs(contract): weak_signal 이 무엇인지 세 프롬프트가 말하게 한다"
```

---

## Task 5: 훼손 실험과 최종 검증

**Files:** 없음 (검증만). 훼손은 반드시 되돌린다.

**Interfaces:**
- Consumes: Task 1~4 전부
- Produces: 훼손 목록과 각각의 결과. 안 잡힌 것마다 테스트를 추가한다.

> ⚠️ **되돌리는 방법.** `git checkout -- <파일>` 을 쓰지 않는다 - 같은 파일에 커밋 안 된
> 작업이 있으면 그것까지 날아간다(실제로 당했다). **원본을 메모리에 보관했다가 `finally`
> 로 복원**하는 드라이버를 쓰고, 배치(4~5건)마다 `git status --short` 로 확인한다.
> 파일이 CRLF 이므로 여러 줄 검색어는 `\n` 을 `\r\n` 으로 바꿔 찾고, 읽기·쓰기 둘 다
> `newline=""` 으로 연다.

- [ ] **Step 1: 시작 전 상태를 확인한다**

Run: `git status --short`
Expected: 미추적 문서 2개 외에 아무것도 없다. 아니면 멈추고 보고한다.

- [ ] **Step 2: 훼손을 하나씩 넣고 스위트를 돌린다**

각 훼손마다: 고친다 → `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=no -rf` → 결과 기록 → 복원.

| # | 파일 | 훼손 | 잡아야 하는 이유 |
|---|---|---|---|
| 1 | `evidence.py` | `residuals()` 에서 `not c.passes` 제거 | 통과 근거가 잔차로 두 번 실린다 |
| 2 | `evidence.py` | `RESIDUAL_MIN_SCORE` 조건 제거 | 0.05 짜리가 `[잔차]` 로 찍힌다 |
| 3 | `evidence.py` | `target_pass` 하한 제거 | 타깃 1장짜리가 근거란에 온다 |
| 4 | `evidence.py` | `kind != "sensor"` 제거 | 미통과 센서가 잔차로 샌다 |
| 5 | `evidence.py` | `statuses.get(c.tool) == "ok"` 제거 | "볼 것이 없었다" 가 "약한 신호" 로 둔갑한다 |
| 6 | `evidence.py` | `passing()` 이 `not c.passes` 도 담게 | **잔차로 confirmed 가 나간다** |
| 7 | `evidence.py` | `ranked_groups` 기본값을 `self.residuals()` 로 | 확정 경로가 조용히 바뀐다 |
| 8 | `nodes.py` | (2a)를 (2) **뒤로** 옮긴다 | 혼합 상태가 no_signal 로 빠져 잔차가 소각된다 |
| 9 | `nodes.py` | (2a)의 `not claim_id` 제거 | 환각 지목이 물러섬으로 둔갑한다 |
| 10 | `nodes.py` | (2a)의 `_record_evidence` 를 `residuals` 만으로 | 통과한 센서 근거가 사라진다 |
| 11 | `nodes.py` | `step_back` 에서 `opens_weak` 제거 | 물러설 길을 못 찾아 루프를 다 태운다 |
| 12 | `nodes.py` | `report_node` 의 `label` 을 `"근거"` 고정 | 미통과가 근거와 글자 하나 다르지 않다 |
| 13 | `nodes.py` | 줄 끝 `reject_reason` 출력 제거 | 왜 약한지 못 읽는다 |
| 14 | `nodes.py` | `ANALYZE_SYSTEM_PROMPT` 의 새 줄 제거 | 계약의 반쪽이 사라진다 |
| 15 | `client.py` | mock `weak_signal` 분기 제거 | 새 판정이 `else` 로 떨어져 확정처럼 찍힌다 |
| 16 | `client.py` | 운영 sys 프롬프트의 새 문장 제거 | 운영 경로에서만 계약이 사라진다 |

- [ ] **Step 3: 안 잡힌 훼손마다 테스트를 추가한다**

훼손을 넣었는데 초록이면 **그 자리는 잠겨 있지 않다.** 실패하는 테스트를 먼저 쓰고
(훼손 상태에서 빨강), 훼손을 되돌리고(초록), 커밋한다. 특히 **6·8·10·11** 은 이 계획의
핵심 안전판이므로 안 잡히면 반드시 추가한다.

- [ ] **Step 4: 워킹 트리가 깨끗한지 확인한다**

Run: `git status --short && git diff --stat`
Expected: 둘 다 비어 있다(미추적 문서 2개 제외).

- [ ] **Step 5: 확정 경로가 안 변했는지 실행으로 확인한다**

Run: `PYTHONIOENCODING=utf-8 python main.py 2>&1 | grep -c "잔차"`
Expected: **0**. 더미 데이터는 1단이 통과하므로 판정이 confirmed 이고 잔차 줄이 없어야
한다 - 있으면 (2a)의 하한이 확정 경로를 침범한 것이다. 실행 자체가 exit 0 인지도 함께
본다(`PYTHONIOENCODING=utf-8 python main.py > NUL 2>&1; echo $?`). 임시 파일이 필요하면
저장소가 아니라 세션 scratchpad 에 쓴다.

- [ ] **Step 6: 최종 스위트**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short 2>&1 | tail -5`
Expected: 전부 통과. 최종 개수를 기록한다 (새 기준선이 된다).

- [ ] **Step 7: 훼손 결과를 커밋한다 (테스트를 추가했다면)**

```bash
git add tests/
git commit -m "test(residual): 훼손 실험에서 안 잡힌 자리를 잠근다"
```

---

## 완료 후

1. `superpowers:requesting-code-review` 로 리뷰를 받는다 (리뷰 Opus 5).
   리뷰어에게 **"규칙을 가르면 그 규칙의 소비자를 전부 찾았는가"** 를 특히 보게 한다 -
   A 의 Critical 이 정확히 그 유형이었다(`llm/client.py::_top_ranked`).
2. 리뷰 지적을 반영한 뒤 `superpowers:finishing-a-development-branch` 로 main 에 ff 병합한다.
3. 메모리(`sensor-claim-eligibility-merged.md` 옆에 새 파일)를 갱신한다: 새 기준선 커밋·
   테스트 수, 확정 계약, ⏭ 다음 = **D/E (어휘·서술)**.
