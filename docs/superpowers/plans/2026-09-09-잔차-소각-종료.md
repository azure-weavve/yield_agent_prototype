# 잔차 소각 종료 + M4(no_separation) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 잔차가 타는 마지막 두 경로((4) 루프 한계 · 게이트 미경유 백스톱)를 막고, "전축을 다 봤는데 아무것도 안 갈렸다" 를 말하는 판정 `no_separation` 을 신설한다.

**Architecture:** 합집합 결정을 헬퍼 하나(`_evidence_groups`)로 모아 네 자리가 같은 규칙을 쓰게 한다((2a)·(2b)·(4)·백스톱 — (2b) 는 Task 4 에서 추가됐다). `_fold_key`(C 브랜치 확정 계약)는 건드리지 않는다 - "지목 가능한 통과 후보가 있으면 잔차를 안 싣는다"(B §10)를 네 자리 모두에서 지키면 통과 claim 과 잔차가 한 묶음에 섞이는 일 자체가 안 일어난다. M4 는 `(2a)` 바로 뒤 `(2)` 앞에 새 분기로 들어가고, 판정 상태 판별은 `_no_separation_state` 하나에만 적는다(게이트와 물러섬 안내가 같은 것을 본다).

**Tech Stack:** Python 3.11 · pytest · LangGraph(`graph/nodes.py`) · mock/운영 LLM 클라이언트(`llm/client.py`)

**Spec:** `docs/superpowers/specs/2026-09-09-잔차-소각-종료-design.md`

## Global Constraints

- 브랜치: `feat/residual-burn-exit` (이미 생성됨, 기준 `978cbac` / 502 passed).
- 주석·독스트링·판정 문구는 **한국어**. 기존 파일의 어투(왜 그런지를 적는다)를 따른다.
- 판정 이름은 `no_separation`. 게이트 주석에서는 `(2b)` 로 부른다.
- 등록 가설 축 4개: `hyp_eqp_ch_commonality` · `hyp_ppid_commonality` · `hyp_step_passage_commonality` · `hyp_metro_commonality`.
- 임계값: `ya_config.RESIDUAL_MIN_SCORE = 0.25` · `ya_config.MAX_LOOPS = 7`. **값은 안 바꾼다.**
- 테스트 실행: 저장소 루트에서 `python -m pytest`.
- 커밋 메시지 끝에 다음 두 줄을 붙인다:

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4
```

- **하나의 규칙을 두 자리에 적지 않는다.** 이 저장소가 반복해 겪은 결함이 "N 자리 중 N-1 자리만 고침" 이다.

---

## File Structure

| 파일 | 역할 | 이 계획에서 하는 일 |
|---|---|---|
| `graph/nodes.py` | 게이트·리포트 노드 | 헬퍼 2개 신설, `(2a)`·`(4)`·백스톱이 헬퍼를 쓰게, M4 분기 신설, 물러섬 안내·프롬프트·주석 |
| `llm/client.py` | mock 결론문 · 운영 리포트 프롬프트 | `no_separation` 분기와 지시문, `inconclusive` 문구 보강 |
| `tests/test_graph_nodes.py` | 게이트·리포트 노드 시험 | 픽스처 `ALL_WEAK` 신설, 신규 테스트 11개 |
| `tests/test_mock_llm.py` | LLM 계약 시험 | 신규 테스트 4개 |

`graph/evidence.py` 는 **안 건드린다**(`_fold_key`·`ranked_groups` 그대로). `domain/hypotheses.yaml` 도 안 건드린다 - 잔차도 M4 도 도구가 발급하는 것이 아니라 게이트가 만드는 출력이다.

---

## Task 1: 합집합 결정을 헬퍼 하나로 모은다

**Files:**
- Modify: `graph/nodes.py` (`_finalize_gate` 위에 헬퍼 신설 · `(2a)` 분기 `:450`)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Produces: `nodes._evidence_groups(bundle, passing_groups) -> list[ClaimGroup]` - 근거로 실을 묶음 목록. 잔차를 안 더할 때는 **인자로 받은 `passing_groups` 객체를 그대로 돌려준다**(호출부가 `is` 로 `picked` 와 짝을 맞출 수 있어야 한다).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_graph_nodes.py` 맨 끝에 추가한다.

```python
def test_evidence_groups_adds_residuals_when_nothing_passes():
    """지목 가능한 통과 후보가 없으면 잔차를 더한 목록을 돌려준다."""
    from graph import evidence

    bundle = evidence.build_bundle([EQP_CH_BELOW_LINE, SENSOR_FINDING])
    passing = bundle.ranked_groups()
    out = nodes._evidence_groups(bundle, passing)
    ids = {c.claim_id for g in out for c in g.claims}
    assert "eqp_ch_commonality:chamber:CC002000:ETCH9_B" in ids   # 잔차
    assert "sensor:CC002000:TEMP_1" in ids                        # 통과 센서도 남는다


def test_evidence_groups_keeps_passing_only_when_a_statistical_claim_passed():
    """통과 후보가 있으면 잔차를 안 더한다 - 접기 계약(_fold_key)의 전제다.

    같은 목록에 통과 claim 과 잔차가 섞이면 p 가 작은 잔차가 lead 를 뺏어
    통과 근거가 `passes` 키도 없는 `confounded_with` 로 강등되고 묶음 전체가
    `[잔차]` 로 찍힌다. 그리고 **같은 객체**를 돌려줘야 호출부의 `picked` 가
    `is` 로 맞는다 - 새로 만들면 아무 claim 에도 picked_by_llm 이 안 붙는다.
    """
    from graph import evidence

    bundle = evidence.build_bundle([EQP_CH_PASSING_AND_RESIDUAL])
    passing = bundle.ranked_groups()
    out = nodes._evidence_groups(bundle, passing)
    assert out is passing
    ids = {c.claim_id for g in out for c in g.claims}
    assert "eqp_ch_commonality:chamber:CD004000:PHOTO1_A" not in ids
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k evidence_groups -v`
Expected: FAIL - `AttributeError: module 'graph.nodes' has no attribute '_evidence_groups'`

- [ ] **Step 3: 헬퍼를 만든다**

`graph/nodes.py` 의 `_finalize_gate` 정의 **바로 위**에 추가한다.

```python
def _evidence_groups(bundle, passing_groups: list) -> list:
    """근거로 실을 묶음. **지목 가능한 통과 후보가 없을 때만 잔차를 더한다.**

    이 하한이 곧 접기 계약의 전제다 - 합집합의 비센서 claim 이 전부 미통과라야
    `_fold_key` 가 통과 claim 과 잔차를 한 묶음에 섞지 않는다(B 설계 §4 · C 계약).
    하한을 지우면 p 가 작은 잔차가 lead 를 뺏어, 통과 근거가 `passes` 키도 없는
    `confounded_with` 로 강등돼 묶음 전체가 `[잔차]` 로 찍힌다 - 통과 근거가
    있는데도.

    **잔차를 안 더할 때는 받은 목록을 그대로 돌려준다.** `ranked_groups()` 는
    호출마다 새 `ClaimGroup` 을 만들어(`find_group` docstring 의 경고) 다시
    만들면 호출부의 `picked` 와 `is` 비교가 항상 거짓이 되고, 아무 claim 에도
    `picked_by_llm` 이 안 붙는다.

    네 자리((2a)·(2b)·(4)·백스톱)가 같은 규칙을 쓴다. 규칙을 네 번 적으면 한 자리를
    빠뜨리는 것이 이 저장소의 반복 결함이다.
    """
    residuals = bundle.residuals()
    if bundle.statistical_passing() or not residuals:
        return passing_groups
    return bundle.ranked_groups(bundle.passing() + residuals)
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k evidence_groups -v`
Expected: PASS (2 passed)

- [ ] **Step 5: `(2a)` 를 헬퍼로 옮긴다**

`graph/nodes.py` 의 `(2a)` 분기에서 아래 줄을 바꾼다. **동작은 같다** - 그 분기의 하한이 곧 헬퍼의 조건이다.

```python
        # 바꾸기 전
        _record_evidence(update, bundle.ranked_groups(bundle.passing() + residuals), None)
        # 바꾼 뒤
        _record_evidence(update, _evidence_groups(bundle, groups), None)
```

- [ ] **Step 6: 회귀가 없음을 확인한다**

Run: `python -m pytest`
Expected: 504 passed (기존 502 + 신규 2)

- [ ] **Step 7: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "refactor(gate): 잔차를 더할지 정하는 규칙을 헬퍼 한 곳으로 모은다

네 자리가 같은 결정을 하는데 (2a) 에만 적혀 있었다. (2b)·(4) 와 백스톱이
같은 규칙을 쓰기 전에 결정 자리를 하나로 만든다 - 규칙을 네 번 적으면
한 자리를 빠뜨리는 것이 이 저장소의 반복 결함이다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4"
```

---

## Task 2: (4) 루프 한계가 잔차를 싣는다

**Files:**
- Modify: `graph/nodes.py` (`(4)` 분기 `:547` 근처)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: `nodes._evidence_groups(bundle, passing_groups)` (Task 1)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_loop_limit_still_carries_residuals():
    """루프 한계로 끝나도 아랫선을 넘은 잔차는 리포트에 남아야 한다.

    실측(2026-09-08): 잔차 2건이 있는데 환각 지목을 되풀이해 loop 7 에 닿으면
    `inconclusive` · `final_claims=0` 으로 끝나 증거가 통째로 소각됐다.
    이 경로는 `(2a)` 가 환각을 안 받아 주기 때문에 열린다 - 프롬프트로는 못 막는다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:NOPE",
         "hypothesis": "지어낸 것", "confidence": 0.9},
        loop=ya_config.MAX_LOOPS, update=update, findings=[EQP_CH_BELOW_LINE])
    assert update["finalize_status"] == "inconclusive"
    ids = [c["claim_id"] for c in update["final_claims"]]
    assert ids == ["eqp_ch_commonality:chamber:CC002000:ETCH9_B"]
    # 판정문이 "확정 근거 없이" 라고 말하면 잔차를 싣고도 거짓이다.
    assert "확정 근거 없이" not in verdict, verdict
    assert "잔차 1건" in verdict, verdict


def test_loop_limit_without_residuals_keeps_the_old_sentence():
    """잔차가 없으면 옛 판정문 그대로다 - 없는 잔차를 말하면 안 된다."""
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "", "hypothesis": "h", "confidence": 0.3},
        loop=ya_config.MAX_LOOPS, update=update, findings=[EQP_CH_SILENT])
    assert update["finalize_status"] == "no_signal"       # (2)가 먼저 받는다
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "지어낸:claim:id", "hypothesis": "h", "confidence": 0.9},
        loop=ya_config.MAX_LOOPS, update=update, findings=[EQP_CH_SILENT])
    assert update["finalize_status"] == "inconclusive"
    assert "확정 근거 없이" in verdict, verdict
    assert "잔차" not in verdict, verdict
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k loop_limit -v`
Expected: `test_loop_limit_still_carries_residuals` 가 FAIL - `assert [] == ['eqp_ch_commonality:chamber:CC002000:ETCH9_B']`

- [ ] **Step 3: `(4)` 를 고친다**

```python
    # (4) 루프 한계 도달 강제 종료는 승인이 아니라 '미확정'
    if loop >= ya_config.MAX_LOOPS:
        update["finalize_accepted"] = True
        update["finalize_status"] = "inconclusive"
        update["final_hypothesis"] = hypothesis
        update["final_confidence"] = conf
        update["coverage"] = coverage
        # **잔차도 싣는다.** 이 경로는 `(2a)` 가 환각·대체 이름을 안 받아 줄 때
        # 열리므로 게이트 협조로는 못 막는다 - 여기서 안 실으면 "봤고 후보도
        # 났는데 약하다" 가 통째로 소각된다(실측 재현).
        # `picked` 는 잔차를 실은 목록에서는 안 붙인다: 그 상태에서 지목할 수
        # 있는 것은 센서나 잔차뿐이고, 그것을 서술의 축으로 삼으면 리포트가 약한
        # 후보를 단정한다((2a)와 같은 이유). 목록이 바뀌면 `is` 비교도 어차피
        # 안 맞는다.
        carried = _evidence_groups(bundle, groups)
        _record_evidence(update, carried, picked if carried is groups else None)
        if carried is not groups:
            return (f"미확정 (루프 한계 도달): 판별선을 넘은 후보를 확정하지 못했다. "
                    f"아랫선을 넘은 잔차 {len(residuals)}건을 근거로 싣는다.")
        return "미확정 (루프 한계 도달): 확정 근거 없이 리포팅으로 진행한다."
```

[2026-09-11 갱신: **위 스니펫 주석의 "환각·대체 이름"** 과 **아래 이 Task 의 커밋
메시지 인용("(2a) 는 환각·대체 이름을 안 받아 주므로")** 은 이제 **환각뿐**이다 -
하한이 `_honest_pick` 으로 넓어져 대체(superseded)된 이름은 `(2a)` 가 받는다.
기록이라 원문은 그대로 둔다.]

[Task 7 갱신: 이 Task 2 시점에는 `carried is not groups` 한 갈래뿐이었다. Task 6
리뷰(I-2)가 절단 전 `len(residuals)` 가 상한 절단에서 거짓이 될 수 있음을 잡아,
지금 코드는 **3갈래**다(최종 리뷰 Minor M-2 정정 - 아래 두 갈래만 부르면 반쪽이다):
`carried is not groups`(잔차가 더해진 갈래 - 실제로 실린 잔차 수를 말한다) ·
`elif carried:`(잔차 없이 통과 근거만 실린 갈래 - "실제로 실린 수"는 이 갈래가
말한다) · `else:`(근거가 정말 없어 "확정 근거 없이"를 말하는 갈래).]

[최종 리뷰 갱신(I-4): `elif carried:` 갈래의 건수도 낡았다 - `len(carried)` 는
절단 **전** 개수라, `_record_evidence` 의 상한이 걸리면 리포트에 없는 근거를
가리키는 판정문이 나간다(재현: 통과 묶음 11개 -> `final_claims` 8건인데 판정문은
"근거 11건"). 지금은 `len(update['final_claims'])` 다.]

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k loop_limit -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 전체 스위트를 돌린다**

Run: `python -m pytest`
Expected: 506 passed

- [ ] **Step 6: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "fix(gate): 루프 한계로 끝나도 잔차를 근거로 싣는다

(2a) 는 환각·대체 이름을 안 받아 주므로 그 되풀이는 (4) 로 떨어진다.
거기서 통과 후보만 싣던 탓에 잔차 2건이 있는 분석이 final_claims=0 으로
끝났다. 판정문의 '확정 근거 없이' 도 잔차를 실은 뒤에는 거짓이라 같이 고친다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4"
```

---

## Task 3: 게이트 미경유 백스톱이 잔차를 싣는다

**Files:**
- Modify: `graph/nodes.py` (`report_node` `:847` 근처)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: `nodes._evidence_groups(bundle, passing_groups)` (Task 1)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_report_node_keeps_residuals_when_the_gate_never_judged():
    """게이트를 아예 안 타는 종료(finalize 미호출)에서도 잔차는 남아야 한다.

    이 경로는 **게이트 협조와 무관하다** - LLM 이 finalize 를 안 부르면 여기로
    오므로 프롬프트로는 못 막는다. `[잔차]` 라벨은 항목의 passes 로 갈리므로
    싣기만 하면 옳게 찍힌다.
    """
    out = nodes.report_node({"target_wafers": ["W1"], "target_source": "manual",
                             "target_group": ["W1"], "status_summary": "s",
                             "findings": [EQP_CH_BELOW_LINE]})
    assert out["finalize_status"] == "inconclusive"
    assert "[잔차 1]" in out["report"], out["report"]
    assert "eqp_ch_commonality:chamber:CC002000:ETCH9_B" in out["report"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k report_node_keeps_residuals -v`
Expected: FAIL - `assert '[잔차 1]' in ...` (근거 줄이 0줄이다)

- [ ] **Step 3: 백스톱을 고친다**

`report_node` 안의 게이트 미경유 분기를 바꾼다.

```python
        gateless = {}
        # (4)와 같은 이유로 잔차도 싣는다. 이 경로는 게이트 협조와 무관해
        # 프롬프트로는 못 막는 유일한 자리다.
        _record_evidence(gateless, _evidence_groups(bundle, bundle.ranked_groups()), None)
        claims = gateless["final_claims"]
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k report_node_keeps_residuals -v`
Expected: PASS

- [ ] **Step 5: 전체 스위트를 돌린다**

Run: `python -m pytest`
Expected: 507 passed

- [ ] **Step 6: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "fix(report): 게이트를 안 타고 끝난 분석도 잔차를 싣는다

LLM 이 finalize 를 아예 안 부르면 백스톱으로 오는데, 거기가 통과 후보만
넘기고 있었다. 게이트 협조와 무관한 경로라 프롬프트로는 못 막는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4"
```

---

## Task 4: M4 - `no_separation` 판정 신설

**Files:**
- Modify: `graph/nodes.py` (`_no_separation_state` 신설 · `(2a)` 뒤 `(2)` 앞에 분기 · `_finalize_gate` docstring 판정 목록)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Produces: `nodes._no_separation_state(bundle, coverage) -> bool` - claim_id 하한을 **뺀** M4 상태 판정. 게이트와 물러섬 안내(Task 5)가 같은 것을 본다.

- [ ] **Step 1: 픽스처를 만든다**

`tests/test_graph_nodes.py` 의 `EQP_CH_PASSING_AND_RESIDUAL` 아래에 추가한다.

```python
def _weak_finding(tool, hyp, claim_id, loop, score):
    """status 는 ok 인데 점수가 잔차 아랫선(0.25) 아래인 후보 하나짜리 실행.

    `status: "ok" if candidates else "no_signal"`(tools/commonality.py) 이므로
    ok 는 곧 "후보가 났다" 이고, 점수가 아랫선 아래라 잔차 자격은 없다.
    """
    return {
        "loop": loop, "tool": tool, "args": {},
        "result": {"hypothesis_id": hyp, "status": "ok", "candidates": [
            {"claim_id": claim_id, "step_seq": "CC002000", "key": "K",
             "level": "chamber", "passes": False,
             "reject_reason": f"분리 점수 {score} < 0.5", "score": score,
             "target_pass": 4, "target_total": 4,
             "control_pass": 3, "control_total": 5},
        ]},
        "thought": "약한 후보",
    }


# 전축을 다 돌렸는데 후보가 전부 아랫선 미만인 상태 (M4). 최고 점수는 0.18 이다 -
# 하나만 다른 값을 주어야 판정문이 '최고' 를 실제로 고르는지 잠글 수 있다.
ALL_WEAK = [
    _weak_finding("hyp_eqp_ch_commonality", "eqp_ch_commonality",
                  "eqp_ch_commonality:chamber:CC002000:ETCH9_B", 2, 0.18),
    _weak_finding("hyp_ppid_commonality", "ppid_commonality",
                  "ppid_commonality:ppid:CC002000:P1", 3, 0.10),
    _weak_finding("hyp_step_passage_commonality", "step_passage_commonality",
                  "step_passage_commonality:step:CC002000:S1", 4, 0.10),
    _weak_finding("hyp_metro_commonality", "metro_commonality",
                  "metro_commonality:item:CC002000:M1", 5, 0.10),
]
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

```python
def test_all_axes_run_but_nothing_separates_ends_as_no_separation():
    """전축을 다 봤는데 아무것도 안 갈리면 출구가 루프 한계뿐이던 것을 고친다 (M4).

    실측(2026-09-08): 전축 ok · 통과 0 · 잔차 0 이면 (2)(3)(3b) 어느 것도 안 열려
    loop 7 까지 왕복하다 inconclusive 로 끝났다 - 엔지니어는 "미확정(루프 한계
    도달)" 을 보는데 실제로 일어난 일은 "다 봤고 아무것도 안 갈렸다" 다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "", "hypothesis": "h", "confidence": 0.3},
        loop=2, update=update, findings=ALL_WEAK)
    assert update["finalize_status"] == "no_separation"
    assert update["finalize_accepted"] is True
    assert "갈리는 항목 없음" in verdict, verdict
    # 최고 점수를 수치로 싣는다 - "아깝게 몰랐다" 와 "흔적도 없다" 를 엔지니어가
    # 스스로 가르는 재료다. 건수만 세면 지워도 스위트가 초록이다.
    assert "최고 분리 점수 0.18" in verdict, verdict
    assert "잔차 아랫선(0.25)" in verdict, verdict
    assert "등록 축 4개 중 4개 대조" in verdict, verdict
    assert "lot 밖 대조군" in verdict, verdict


def test_no_separation_wins_over_no_signal():
    """한 축은 침묵하고 다른 축은 약한 후보를 낸 상태에서 둘 다 참이다.

    (2) 뒤에 두면 **빈손 제출은 no_signal, 지목한 제출은 no_separation** 으로
    같은 증거가 제출 형태에 따라 다른 판정을 받는다. (2a)를 (2) 앞에 둔 것과
    같은 원칙 - 정보가 더 많고 조건이 더 좁은 쪽이 이긴다.
    """
    update = {}
    nodes._finalize_gate({"claim_id": "", "hypothesis": "h", "confidence": 0.3},
                         loop=2, update=update,
                         findings=[ALL_WEAK[0], PPID_SILENT,
                                   STEP_PASSAGE_SILENT, METRO_SILENT])
    assert update["finalize_status"] == "no_separation"


def test_all_silent_is_still_no_signal():
    """후보가 하나도 안 난 상태는 M4 가 아니다 - '갈리지 않았다' 가 아니라
    '볼 것이 안 났다' 이고, 그것을 말하는 판정은 이미 (2)다."""
    update = {}
    nodes._finalize_gate({"claim_id": "", "hypothesis": "h", "confidence": 0.3},
                         loop=2, update=update, findings=ALL_SILENT)
    assert update["finalize_status"] == "no_signal"


def test_no_separation_requires_every_axis():
    """부분 커버리지로 열면 근거 0건짜리 리포트로 조기 종료한다.

    '더 볼 것이 없었다' 는 (3)과 같은 성격의 **주장**이라 전축을 봐야 참이다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "", "hypothesis": "h", "confidence": 0.3},
        loop=2, update=update, findings=[ALL_WEAK[0]])
    assert "finalize_status" not in update
    assert "반려" in verdict, verdict


def test_no_separation_accepts_an_honest_pick():
    """약한 후보를 지목했다고 문을 닫으면 반려를 되풀이하다 루프 한계로 빠져
    이번에 고치는 결함이 그대로 재발한다((2a)와 같은 규칙)."""
    update = {}
    nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:ETCH9_B",
         "hypothesis": "ETCH9_B 편중", "confidence": 0.9},
        loop=2, update=update, findings=ALL_WEAK)
    assert update["finalize_status"] == "no_separation"
    # 받아준 것이지 원인으로 확정한 것이 아니다.
    assert not any(c.get("picked_by_llm") for c in update["final_claims"])


def test_a_made_up_claim_id_does_not_open_no_separation():
    """환각은 '갈리지 않았다' 와 다른 사실이라 같은 이름을 주면 안 된다."""
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:NOPE",
         "hypothesis": "지어낸 것", "confidence": 0.9},
        loop=2, update=update, findings=ALL_WEAK)
    assert "finalize_status" not in update
    assert "도구 결과에 없다" in verdict, verdict
```

- [ ] **Step 3: 실패를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k "no_separation or all_silent or nothing_separates" -v`
Expected: 4 FAIL (판정이 안 열려 반려로 떨어진다) · `test_all_silent_is_still_no_signal` 과 `test_a_made_up_claim_id_does_not_open_no_separation` 은 PASS(회귀 방지용이라 지금도 참이다)

- [ ] **Step 4: 상태 판정 함수를 만든다**

`graph/nodes.py` 의 `_evidence_groups` 옆에 추가한다.

```python
def _no_separation_state(bundle, coverage: dict) -> bool:
    """(2b) '갈리는 항목 없음' 이 성립하는 상태인가 - **claim_id 하한은 빼고**.

    게이트 판정과 물러섬 안내(`_no_candidate_action`)가 **같은 것을 봐야 한다.**
    판정만 만들고 안내를 안 고치면 그 상태에서 "claim_id 를 비우고 finalize
    하라" 가 안 붙어 문이 열려 있는 줄도 모르고 루프 한계까지 왕복한다 -
    (3)이 실제로 겪었던 라이브락이다.

    `not unrun and not failed`: "더 볼 것이 없었다" 는 (3)과 같은 성격의 주장이라
    전축을 봐야 참이다. 부분 커버리지로 열면 근거 0건짜리 리포트로 조기 종료해
    있을지 모를 신호를 스스로 달아난다.

    `any(kind != "sensor")`: **후보가 하나도 안 난 상태(전축 no_signal)는 (2)다.**
    `status: "ok" if candidates else "no_signal"` 이므로 이 조건은 "1단 후보가
    실제로 났다" 와 같은 뜻이고, 판정문의 최고 점수를 쓸 수 있다는 보장도 된다.
    """
    return (not coverage["unrun"] and not coverage["failed"]
            and not bundle.statistical_passing()
            and not bundle.residuals()
            and any(c.kind != "sensor" for c in bundle.claims.values()))
```

- [ ] **Step 5: `(2a)` 바로 뒤, `(2)` 앞에 분기를 넣는다**

```python
    # (2b) 갈리는 항목 없음 - 전축을 대조했는데 후보가 전부 아랫선 미만이다.
    #      "봤는데 아무것도 안 갈렸다" 를 말하는 판정이 없어서, 이 상태는 출구가
    #      루프 한계뿐이었다 - 엔지니어는 실제로 일어난 일과 다른 사유("미확정 -
    #      루프 한계 도달")를 본다(실측 재현, 조사 §2.2).
    #
    #      **(2)보다 앞이다.** 한 축은 no_signal, 다른 축은 ok + 약한 후보인 상태에서
    #      두 조건이 동시에 참인데, (2) 뒤에 두면 빈손 제출은 no_signal 이고 지목한
    #      제출은 no_separation 이라 **같은 증거가 제출 형태에 따라 다른 판정**을
    #      받는다. (2a)를 (2) 앞에 둔 것과 같은 원칙이다.
    #      (3)과는 배타적이다 - (3)은 status 가 전부 NO_DATA 일 때만 열리는데
    #      여기는 ok 인 축을 요구한다. 그래서 (3) 앞뒤는 아무 효과가 없다.
    #
    #      하한이 "정직한 제출" 인 이유는 (2a)와 같다 - 약한 후보를 지목했다고 문을
    #      닫으면 반려를 되풀이하다 루프 한계로 빠져 이 판정이 없애려는 상태가 그대로
    #      재발한다. 환각만 반려한다.
    if _no_separation_state(bundle, coverage) and (not claim_id or claim is not None):
        update["finalize_accepted"] = True
        update["finalize_status"] = "no_separation"
        update["final_hypothesis"] = hypothesis
        update["final_confidence"] = conf
        update["coverage"] = coverage
        # 정의상 통과 후보도 잔차도 없어 목록은 비거나 통과 센서만 남는다.
        # picked 는 안 붙인다 - (2a)와 같은 이유로 원인으로 확정하지 않는다.
        _record_evidence(update, _evidence_groups(bundle, groups), None)
        # 최고 점수는 **비센서에서만** 뽑는다. 센서는 자기 판별선을 쓰는 다른
        # 눈금이라 같은 줄에 놓으면 "더 센데 졌다" 로 읽힌다.
        top = max(c.score for c in bundle.claims.values() if c.kind != "sensor")
        return (f"갈리는 항목 없음 ({_coverage_phrase(coverage)}): 타깃과 대조군을 "
                f"가르는 항목이 없다. 최고 분리 점수 {top:.2f} 로 잔차 "
                f"아랫선({ya_config.RESIDUAL_MIN_SCORE})에도 미달한다. 분석이 안 돌은 "
                f"것도 근거가 약한 것도 아니라 lot 내부 대조로는 갈리지 않는다는 "
                f"뜻이다. lot 밖 대조군 또는 다른 관측축이 필요하다. 리포팅으로 "
                f"진행한다.")
```

[2026-09-11 갱신: **위 스니펫의 하한 `(not claim_id or claim is not None)` 과 그 위
주석의 "환각만 반려한다" 도 낡았다.** 의도("환각만")는 맞았는데 술어가 그것을 안
지켰다 - `bundle.claims` 가 대체(superseded)된 후보를 안 담아 환각이 아닌 제출까지
걸렸다. 지금은 `_honest_pick(bundle, claim_id)` 다. 기록이라 원문은 그대로 둔다.]

[최종 리뷰 갱신: 위 판정문은 낡았다. 최종 리뷰(I-1·I-3(a))가 "타깃과 대조군을
가르는 항목이 없다" 를 **가설 도구(hyp_*) 축**에 한정하고(통과한 2단 센서가 근거로
함께 실린 상태에서 축 전체로 말하면 거짓이라 뒤에 센서 단서 문장이 조건부로
붙는다), `coverage["no_data"]` 가 있으면 "분석이 안 돌은 것도 아니라" 를 빼고
계산 불가 축에 적재 범위·추출 조건 확인 조치를 붙이도록 늘렸다. 지금 문구는
`graph/nodes.py` `(2b)` 분기를 본다.]

- [ ] **Step 6: `_finalize_gate` docstring 의 판정 목록에 한 줄을 더한다**

`(2a)` 줄 **아래**에 넣는다. 목록이 실제 순서와 어긋나면 다음 사람이 순서를 잘못 읽는다.

```
      (2b) 전축 대조 + 통과 0 + 비센서 후보 점수 전부 아랫선(RESIDUAL_MIN_SCORE) 미만
           + 후보는 났음 + 제출이 정직함 -> no_separation
           ((2)보다 앞이다. "볼 것이 안 났다"(2)와 "봤는데 안 갈렸다"는 다른 사실이다.
           [Task 7 갱신: "잔차 0" 은 Task 4 가 Critical 로 폐기한 조건이다 - 표본이
           얇아 미통과인 완전 분리 후보는 잔차에도 안 잡혀 "잔차 0" 이 거짓 판정문을
           낸다. 지금은 점수를 직접 본다.])
```

- [ ] **Step 7: 통과를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k "no_separation or all_silent or nothing_separates" -v`
Expected: PASS (6 passed)

- [ ] **Step 8: 전체 스위트를 돌린다**

Run: `python -m pytest`
Expected: 513 passed. **실패가 나면 그 테스트가 무엇을 단언하는지 먼저 읽는다** - `(2)`/`(4)` 판정을 단언하던 것이 이제 `no_separation` 으로 바뀌는 것은 **의도된 변경**이므로 테스트를 고치되, 독스트링도 새 규칙에 맞게 다시 쓴다(문장이 낡으면 다음 사람이 거짓을 읽는다).

- [ ] **Step 9: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "feat(gate): '봤는데 아무것도 안 갈렸다' 를 말하는 판정을 만든다

전축 ok · 통과 0 · 잔차 0 이면 (2)(3)(3b) 어느 것도 안 열려 출구가 루프
한계뿐이었고, 엔지니어는 실제로 일어난 일과 다른 사유를 봤다. (2) 앞에
두는 이유는 (2a)와 같다 - 같은 증거가 제출 형태에 따라 다른 판정을 받으면
안 된다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4"
```

---

## Task 5: 물러섬 안내가 새 문을 안다

**Files:**
- Modify: `graph/nodes.py` (`_no_candidate_action` `:794` 근처)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: `nodes._no_separation_state(bundle, coverage)` (Task 4)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_no_separation_state_offers_the_step_back_path():
    """판정만 만들고 안내를 안 고치면 문이 열려 있는 줄도 모른다.

    이 상태에서 LLM 이 지어낸 이름을 내면 반려를 받는데, 그 반려가 "claim_id 를
    비우고 finalize 하라" 를 안 붙이면 같은 왕복이 루프 한계까지 이어진다 -
    (3)이 실제로 겪었던 라이브락이고, 그때도 원인은 판정과 안내가 다른 것을
    보고 있었기 때문이다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:NOPE",
         "hypothesis": "지어낸 것", "confidence": 0.9},
        loop=2, update=update, findings=ALL_WEAK)
    assert "claim_id 를 비우고" in verdict, verdict
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k step_back_path -v`
Expected: FAIL - 안내 문장이 없다

- [ ] **Step 3: 안내를 고친다**

`_no_candidate_action` 안에서 `step_back` 을 계산하는 부분을 바꾼다.

```python
    opens_tool_failure = bool(failed) and not unrun and uncomputable     # (3b)
    opens_no_separation = _no_separation_state(bundle, coverage)         # (2b)
    step_back = (" 지목할 것이 없어 물러설 때는 claim_id 를 비우고 finalize 하라."
                 if opens_no_signal or opens_weak or opens_no_data
                 or opens_tool_failure or opens_no_separation
                 else "")
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -k step_back_path -v`
Expected: PASS

- [ ] **Step 5: 전체 스위트를 돌린다**

Run: `python -m pytest`
Expected: 514 passed

- [ ] **Step 6: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "fix(gate): 새 판정이 여는 문을 물러섬 안내도 알게 한다

판정과 안내가 서로 다른 것을 보면, 문이 열려 있는데도 LLM 이 그 사실을
못 듣고 왕복만 한다. 두 자리가 같은 함수를 본다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4"
```

---

## Task 6: LLM 계약 - mock 결론문 · 운영 프롬프트 · 분석 프롬프트 · 주석

**Files:**
- Modify: `llm/client.py` (mock 결론문 status 분기 `:255~` · 운영 리포트 sys 프롬프트 `:417~440`)
- Modify: `graph/nodes.py` (`ANALYZE_SYSTEM_PROMPT` `:48` · `_gate_rejection` 주석 `:706` · `_record_evidence` docstring)
- Test: `tests/test_mock_llm.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_mock_llm.py` 맨 끝에 추가한다.

```python
def test_mock_report_has_a_sentence_for_no_separation():
    """판정 어휘를 늘리면 mock 결론문도 같이 늘려야 한다 - 안 그러면 새 판정이
    else 로 떨어져 LLM 이 쓴 가설이 확정처럼 찍힌다."""
    from llm.client import ScriptedMockLLMClient
    report = ScriptedMockLLMClient().generate_report(
        target_wafers=["W1"], target_source="manual", target_group=["W1"],
        status_summary="s", findings=[], hypothesis="h", confidence=0.3,
        finalize_status="no_separation")
    assert "갈리는 항목 없음" in report
    assert "lot 밖 대조군" in report


def test_operational_prompt_tells_the_report_what_no_separation_means():
    """운영 리포트 LLM 은 판정 이름만 받는다 - 무엇인지 안 알려주면 '분석 미수행'
    으로 뭉개거나 반대로 '원인 없음' 으로 단정한다. 둘 다 조치가 틀려진다."""
    client = _openai_client()
    client.generate_report(
        target_wafers=["W1"], target_source="manual", target_group=["W1"],
        status_summary="s", findings=[], hypothesis="h", confidence=0.3,
        finalize_status="no_separation", claims=[])
    assert "판정이 no_separation 이면" in client.llm.seen_sys
    assert "분석 미수행이 아니다" in client.llm.seen_sys
    assert "다른 관측축" in client.llm.seen_sys


def test_operational_prompt_says_inconclusive_can_carry_residuals():
    """(4)가 잔차를 싣게 됐으므로 inconclusive 도 [잔차] 줄을 받을 수 있다.

    '확정 근거가 없다' 로만 지시하면 LLM 이 실제로 실린 잔차 줄을 근거 없음과
    모순되는 것으로 보고 지우거나, 반대로 근거로 승격시킨다.
    """
    client = _openai_client()
    client.generate_report(
        target_wafers=["W1"], target_source="manual", target_group=["W1"],
        status_summary="s", findings=[], hypothesis="h", confidence=0.3,
        finalize_status="inconclusive", claims=[])
    assert "inconclusive 에도 잔차가 실릴 수 있다" in client.llm.seen_sys


def test_analyze_prompt_knows_the_full_axis_case_is_received_not_rejected():
    """분석 프롬프트가 '잔차마저 없으면 반려된다' 로만 말하면 이제 반만 참이다.

    등록 축을 다 돌린 뒤라면 게이트가 '갈리는 항목 없음' 으로 받는다. 반쪽짜리
    문장을 남겨 두면 LLM 이 물러설 수 있는 자리에서 축을 더 부르며 예산을 태운다.
    """
    assert "갈리는 항목 없음" in nodes.ANALYZE_SYSTEM_PROMPT
```

[최종 리뷰 갱신(I-2): 위 독스트링과 단언은 낡았다. "등록 축을 다 돌린 뒤라면" 은
`_no_separation_state` 가 실제로 요구하는 조건 - **도구 실패 없이** 다 돌렸을 것과
가설 도구 **후보가 나서** 그 **분리 점수가 전부 아랫선에도 못 미칠 것** - 을
빠뜨렸다(재현: `ALL_THIN` 에 빈손 제출 -> 게이트는 반려·inconclusive 로 끝났는데
프롬프트는 "받는다" 고 말했다). 지금 단언은 그 조건을 다 담은 문장 전체를 본다.]

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_mock_llm.py -k "no_separation or residuals or full_axis" -v`
Expected: 4 FAIL

- [ ] **Step 3: mock 결론문에 분기를 넣는다**

`llm/client.py` 의 `elif finalize_status == "no_signal":` **앞**에 넣는다(가까운 판정끼리 붙여 둔다).

```python
        elif finalize_status == "no_separation":
            conclusion = ("갈리는 항목 없음 - 등록 축을 다 대조했으나 타깃과 대조군을 "
                          "가르는 항목이 없다. 분석이 안 돌은 것도 근거가 약한 것도 "
                          "아니라 lot 내부 대조로는 갈리지 않는다는 뜻이며, "
                          "lot 밖 대조군 또는 다른 관측축이 필요하다.")
```

[최종 리뷰 갱신(I-1·I-3(a)): 위 mock 분기는 낡았다. claims 에 통과한 2단 센서가
있으면(kind 로 판별) "가르는 항목이 없다" 뒤에 센서 단서 문장이 붙고, coverage 에
no_data 축이 있으면 "분석이 안 돌은 것도 아니라" 가 빠지고 계산 불가 축에 적재
범위·추출 조건 확인 조치가 붙는다 - 둘 다 없으면 위 문구 그대로다.]

- [ ] **Step 4: `inconclusive` mock 문구에 잔차를 알린다**

```python
        if finalize_status == "inconclusive":
            conclusion = (f"미확정 (루프 한계 도달) - 유력 가설: {hypothesis or '없음'}. "
                          f"아래 [잔차] 줄이 있으면 판별선을 넘지 못한 후보다.")
```

- [ ] **Step 5: 운영 리포트 sys 프롬프트에 두 문장을 넣는다**

`llm/client.py` 의 판정별 지시문 블록에 추가한다(기존 `tool_failure` 지시문 뒤).

```python
            "판정이 no_separation 이면 '갈리는 항목 없음'으로 서술하라 - "
            "분석 미수행이 아니다. 등록 축을 다 대조했는데 타깃과 대조군을 가르는 "
            "항목이 없었다는 관측이며, 원인이 없다고 단정하지 말고 lot 밖 대조군이나 "
            "다른 관측축이 필요하다고 적어라. "
            "판정이 inconclusive 에도 잔차가 실릴 수 있다 - 확정 근거가 아니라는 "
            "뜻이지 근거가 한 줄도 없다는 뜻이 아니니, [잔차] 항목을 지우지도 "
            "근거로 승격시키지도 마라. "
```

[최종 리뷰 갱신(I-1·I-3(a)): 위 `no_separation` 지시문은 낡았다. "타깃과 대조군을
가르는 항목이 없었다" 를 **가설 도구(hyp_*) 축**에 한정하는 문구와, 2단 센서 근거는
통과했을 수 있으니 그것까지 '가르는 항목이 없다'로 뭉개지 말라는 지시, coverage 에
no_data 축이 있으면 결론을 계산된 축에 한정하고 적재 범위·추출 조건 확인을 후속
조치로 적으라는 지시가 늘었다.]

**커버리지 고백 문장("weak_signal 이거나 no_signal 이거나 inconclusive")에는 `no_separation` 을 안 더한다** - 그 판정은 `not unrun and not failed` 를 요구해 고백할 구멍이 정의상 없다. 더하면 LLM 이 없는 미실행 축을 지어낸다.

- [ ] **Step 6: `ANALYZE_SYSTEM_PROMPT` 를 고친다**

`graph/nodes.py:48` 의 "잔차마저 없는 상태에서 지목하면 반려되고" 부분을 바꾼다.

```
잔차마저 없는 상태에서 지목하면 반려된다 - 다만 등록 축을 다 돌린 뒤라면 게이트가 '갈리는 항목 없음' 으로 받으니 그때는 빈손으로 물러서라. 같은 반려를 되풀이하면 루프 예산만 태운다.
```

[최종 리뷰 갱신(I-2): 위 문구는 낡았다. "등록 축을 다 돌린 뒤라면" 은 반쪽짜리라
`_no_separation_state` 가 실제로 요구하는 조건을 채워, 지금은 "등록 축을 도구 실패
없이 다 돌렸고 가설 도구 후보가 났는데 그 분리 점수가 전부 아랫선에도 못 미치면
게이트가 '갈리는 항목 없음' 으로 받으니 그때는 빈손으로 물러서라" 다.
같은 불릿의 "아랫선을 넘은 잔차가 있으면 ... 그것을 근거로 실은 채 끝나지만" 도
상한 절단에서 거짓이 될 수 있어(Minor 12) "상한이 남는 한 그것을 근거로 싣지만"
으로 고쳤다.]

- [ ] **Step 7: 낡아진 주석 두 곳을 고친다**

`_gate_rejection` (`nodes.py:706`) - "**단 (2a) 는 예외다**" 문단에 한 문장을 더한다.

```
        # **(2b)도 같은 예외다.** 전축을 다 돌렸고 잔차가 하나도 없으면 지목한
        # 제출도 (2b)가 먼저 받는다. 이 분기가 실제로 도는 것은 두 문이 다 닫힌
        # 상태다.
```

[Task 7 갱신: 이 Step 작성 시점의 조건("잔차가 하나도 없으면")은 Task 4 리뷰가
Critical 로 폐기한 옛 조건이다. 지금 코드(`_gate_rejection`, `nodes.py` 886~888
근처)는 "전축을 다 대조했고 비센서 claim 이 있는데 그 점수가 전부 아랫선(0.25)
미만이면" 이다. 이 Step 은 Task 4 수정 라운드가 이미 넣어 두어 Task 6 에서는
따로 하지 않았다.]

`_record_evidence` docstring - "(2a)에서는 ... 잔차도 함께 실린다" 를 고친다.

```
    """판별선을 넘은 근거를 상태에 싣는다 - **지목 가능한 통과 후보가 없으면**
    판별선을 못 넘은 잔차도 함께 실린다((2a)·(4)·백스톱이 `_evidence_groups` 로
    같은 규칙을 탄다). **모든 종료 경로에서 부른다.**
```

[Task 7 갱신: 이 Step 작성 시점의 나열("(2a)·(4)·백스톱")은 세 자리였다. Task 4
가 `(2b)` 를 `_evidence_groups` 의 네 번째 호출부로 만들어, 지금 코드
(`_record_evidence` docstring, `nodes.py` 718 근처)는 "(2a)·(2b)·(4)·백스톱"
네 자리를 나열한다.]

- [ ] **Step 8: 통과를 확인한다**

Run: `python -m pytest tests/test_mock_llm.py -k "no_separation or residuals or full_axis" -v`
Expected: PASS (4 passed)

- [ ] **Step 9: 전체 스위트를 돌린다**

Run: `python -m pytest`
Expected: 518 passed

- [ ] **Step 10: 커밋**

```bash
git add graph/nodes.py llm/client.py tests/test_mock_llm.py
git commit -m "feat(contract): 새 판정과 잔차를 LLM 계약 다섯 자리에 함께 넣는다

판정을 신설하면 대응하는 자리가 정해져 있다 - mock 결론문, 운영 리포트
프롬프트, 분석 프롬프트, 반려 주석, _record_evidence 독스트링. 프롬프트만
고치고 단언을 안 넣으면 아무것도 안 잠긴다(실측).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4"
```

---

## Task 7: 훼손 실험과 전체 grep

훼손(mutation) 실험은 **구현이 끝난 뒤에** 뽑는다. 설계 시점에 표를 쓰면 낡는다(B 브랜치에서 18/18 CAUGHT 를 받고도 최종 리뷰가 안 잠긴 자리 2곳을 더 찾았다).

**Files:**
- Modify: (훼손이 구멍을 드러내면) `tests/test_graph_nodes.py` · `tests/test_mock_llm.py`

- [ ] **Step 1: 훼손 목록을 만든다**

아래 15건을 하나씩 넣고 스위트를 돌린다. **연언 가드는 절반씩 훼손한다** - 통째로 지우면 다른 테스트가 먼저 빨개져 무엇이 안 잠겼는지 안 보인다.

| # | 자리 | 훼손 |
|---|---|---|
| 1 | `_evidence_groups` | `if bundle.statistical_passing() or not residuals:` -> `if not residuals:` |
| 2 | `_evidence_groups` | `return passing_groups` -> `return bundle.ranked_groups()` (새 객체) |
| 3 | `(4)` | `carried = _evidence_groups(...)` -> `carried = groups` |
| 4 | `(4)` | 판정문 분기를 지우고 항상 "확정 근거 없이" |
| 5 | `(4)` | `picked if carried is groups else None` -> `picked` |
| 6 | 백스톱 | `_evidence_groups(bundle, bundle.ranked_groups())` -> `bundle.ranked_groups()` |
| 7 | `_no_separation_state` | `not coverage["unrun"]` 만 지운다 |
| 8 | `_no_separation_state` | `not coverage["failed"]` 만 지운다 |
| 9 | `_no_separation_state` | `not bundle.statistical_passing()` 을 지운다 |
| 10 | `_no_separation_state` | `not bundle.residuals()` 를 지운다 |
| 11 | `_no_separation_state` | `any(c.kind != "sensor" ...)` 를 지운다 |
| 12 | `(2b)` | `(not claim_id or claim is not None)` -> `True` (환각 흡수) |
| 13 | `(2b)` | 분기를 `(2)` **뒤로** 옮긴다 |
| 14 | `(2b)` | `max(...)` 에서 `c.kind != "sensor"` 필터를 뺀다 |
| 15 | `_no_candidate_action` | `or opens_no_separation` 을 지운다 |

- [ ] **Step 2: 훼손을 하나씩 넣고 돌린다**

각 훼손마다: 파일을 고치고 `python -m pytest` 를 돌린 뒤 **반드시 원상 복구**한다. `git checkout` 으로 되돌리지 말 것 - 커밋 안 된 작업까지 날아간다(실제로 날렸다). 훼손 전 원문을 변수에 담아 두고 `finally` 로 되돌리거나, 한 건씩 손으로 되돌린다.

기록 형식: `#N CAUGHT (tests/test_x.py::test_y)` 또는 `#N SURVIVED`.

- [ ] **Step 3: 살아남은 훼손마다 테스트를 더한다**

`SURVIVED` 가 나오면 그 훼손을 잡는 단언을 추가한다. 단언은 **그 훼손이 바꾸는 것**을 정확히 겨눈다(예: #14 는 센서 효과크기 2.31 이 "최고 분리 점수" 로 찍히는지를 본다).

- [ ] **Step 4: 훼손 파일이 안 남았는지 확인한다**

Run: `git diff --stat`
Expected: 훼손이 하나도 안 남아 있어야 한다(Step 3 에서 추가한 테스트만 보인다). **이 확인을 건너뛰면 훼손 파일이 다음 실행의 기준선이 된다** - 실제로 겪었다.

- [ ] **Step 5: 옛 규칙의 문장으로 저장소 전체를 grep 한다**

```bash
grep -rn "ranked_groups()" --include=*.py .
grep -rn "확정 근거 없이" --include=*.py --include=*.md .
grep -rn "(2a) 는 예외\|(2a)만\|(2a) 만" --include=*.py --include=*.md .
grep -rn "no_signal 이거나 inconclusive" --include=*.py .
```

각 히트가 새 규칙에서도 참인지 확인한다. **거짓이 된 문장은 주석·독스트링·설계서까지 고친다** - 기능이 문장을 거짓으로 만드는 것이 이 저장소의 반복 결함이다(B 에서 6개, 지목 종료에서 3자리).

- [ ] **Step 6: 최종 확인**

Run: `python -m pytest`
Expected: 518 + (Step 3 에서 추가한 수) passed, 실패 0

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "test(gate): 훼손 실험으로 드러난 구멍을 잠그고 낡은 문장을 고친다

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpoZwxKJCdC5KZzDg2jPe4"
```

---

## 완료 기준 (설계서 §10)

- [ ] 잔차 2건 · loop=7 · 환각 지목 -> `inconclusive` 이면서 `final_claims` 가 비지 않는다.
- [ ] 게이트 미경유 종료 -> 백스톱이 잔차를 싣고 `[잔차 1]` 로 찍힌다.
- [ ] 전축 ok · 통과 0 · 비센서 후보 점수 전부 아랫선(RESIDUAL_MIN_SCORE) 미만 ->
      `no_separation`, 최고 점수가 판정문에 찍힌다. (설계서의 "잔차 0" 은 Task 4 가
      폐기한 옛 표현이다.)
- [ ] 통과 후보가 있는 상태에서 `(4)` 가 싣는 목록은 지금과 같다(회귀 없음).
- [ ] 전축 no_signal(후보 0건) -> 여전히 `(2) no_signal`.
- [ ] `graph/evidence.py` 는 한 줄도 안 바뀌었다(`git diff --stat` 으로 확인).

병합 전에 코드 리뷰(`superpowers:requesting-code-review`)를 받는다. 이 저장소는 **훼손 실험을 전부 통과하고도 리뷰가 매번 실재 결함을 더 찾았다**(C 8건 · A 7건 · B 6건).
