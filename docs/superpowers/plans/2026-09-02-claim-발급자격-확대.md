# Claim 발급 자격 확대 (2단 센서) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2단 센서(`compare_sensor_distribution`) 후보가 `claim_id` 를 받아 순위·`final_claims`·리포트 `[근거]` 에 실리되, 게이트 승인(confirmed)의 지목 대상은 아니게 한다.

**Architecture:** 센서 도구가 스스로 게이트 계약(`claim_id`/`passes`/`reject_reason`)을 발급하고, `build_bundle` 이 그것을 `kind="sensor"` Claim 으로 투영한다. 센서 status 는 `ran`/`statuses`/폐기 장부에 **넣지 않는다**(그 셋은 등록 축 커버리지 전용 어휘다). 게이트는 `kind` 로 승인 하한을 걸고, 물러섬 판정은 `passing()` 에서 `statistical_passing()` 으로 옮긴다.

**Tech Stack:** Python 3, pytest, LangGraph, frozen dataclass

**Spec:** `docs/superpowers/specs/2026-09-02-claim-발급자격-확대-design.md`

## Global Constraints

- 기준선 **441 passed**. 매 Task 끝에 전체 스위트가 초록이어야 한다.
- 브랜치 `feat/sensor-claim-eligibility` (스펙 커밋 `006b589` 위에 쌓는다).
- 테스트 실행: `python -m pytest -q --tb=short 2>&1 | Select-Object -Last 8` (PowerShell). Bash 라면 `python -m pytest -q --tb=short 2>&1 | tail -8`.
- **콘솔이 cp949 다.** 사용자에게 보내는 메시지에 em-dash(—)를 쓰지 않는다. 소스 파일 안의 한글·기호는 무관하다(UTF-8).
- `SENSOR_PASS_MIN_EFFECT = 0.8` (Cohen's large). env 로 덮을 수 있어야 한다.
- 센서 claim_id 형식: `sensor:{step_seq}:{sensor_name}` — 게이트는 이 문자열을 **파싱하지 않는다**(사전 조회 전용).
- `graph/evidence.py` 는 **판정하지 않는다.** 판별선(`passes`) 계산은 `tools/sensor_compare.py` 가, 승인/반려는 `graph/nodes.py` 가 한다. 이 분업을 깨지 않는다.
- 주석은 **왜 그렇게 했는가**를 적는다. 무엇을 하는지는 코드가 말한다 (기존 파일들의 관례).

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `ya_config.py` | 임계 상수 | `SENSOR_PASS_MIN_EFFECT` 신설 |
| `tools/sensor_compare.py` | 센서 비교 + **게이트 계약 발급** | 결과에 `kind`, 후보에 `claim_id`/`passes`/`reject_reason` |
| `graph/evidence.py` | 도구 결과 → Claim 투영 (판정 안 함) | `Claim.kind` · `_is_sensor_result` · `build_bundle` 분기 · `Bundle.statistical_passing()` · 렌더러 분기 |
| `graph/nodes.py` | 게이트 판정 | 승인 `kind` 하한 · `passing()` 기준 이동 · 센서 지목 반려 분기 |
| `domain/hypotheses.yaml` | LLM 계약 문서 | 4가설에 센서 지목 불가 문장 |
| `llm/client.py` | 운영 sys 프롬프트 | 같은 문장 |
| `tools/agent_tools.py` | LLM 도구 스키마 | `finalize` docstring 한 줄 |

---

## Task 1: 센서 도구가 게이트 계약을 발급한다

**Files:**
- Modify: `ya_config.py:87` (SENSOR_MIN_SAMPLE 바로 아래)
- Modify: `tools/sensor_compare.py:66-84` (후보 생성 루프 + 반환 3곳)
- Test: `tests/test_sensor_compare.py`

**Interfaces:**
- Consumes: 없음 (첫 Task)
- Produces:
  - `ya_config.SENSOR_PASS_MIN_EFFECT: float` (기본 0.8)
  - `compare_sensor_distribution()` 반환 dict 에 최상위 `"kind": "sensor"` 추가 (모든 status 경로에서)
  - 각 후보 dict 에 `"claim_id": str`, `"passes": bool`, `"reject_reason": str | None` 추가

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_sensor_compare.py` 끝에 추가:

```python
def test_candidates_carry_the_gate_contract():
    """센서 후보도 claim_id 를 받는다 - 그래야 게이트가 조회하고 리포트가 인용한다.

    claim_id 에 step_seq 를 넣는 이유: 다른 스텝의 두 번째 호출이 첫 호출의 근거를
    덮어쓰면 안 된다. 두 스텝은 서로를 대체하는 재실행이 아니라 다른 질문이다.
    """
    res = _run()
    assert res["kind"] == "sensor"
    c = res["candidates"][0]
    assert c["claim_id"] == f"sensor:{SENSOR_STEP}:{c['sensor_name']}"
    assert c["passes"] is True                     # 실제 원인 센서는 효과가 크다
    assert c["reject_reason"] is None


def test_weak_effect_is_recorded_but_does_not_pass(monkeypatch):
    """판별선 미달 센서는 목록에 남되 passes=False 다 (1단 미통과 후보와 같은 취급).

    지우면 게이트가 reject_reason 을 돌려줄 수 없고, 그대로 통과시키면 d=0.05 짜리가
    리포트 [근거] 에 올라간다.
    """
    monkeypatch.setattr(ya_config, "SENSOR_PASS_MIN_EFFECT", 99.0)
    cands = _run()["candidates"]
    assert cands, "후보 자체는 여전히 나와야 한다"
    assert all(c["passes"] is False for c in cands)
    assert all("99.0" in c["reject_reason"] for c in cands)


def test_kind_is_present_even_when_nothing_separates():
    """판별자는 모든 경로에 있어야 한다 - 하나라도 빠지면 그 경로의 결과가
    build_bundle 에서 조용히 무시된다."""
    res = sc.compare_sensor_distribution(SENSOR_STEP, GROUP_WAFERS[:1], CONTROL_WAFERS)
    assert res["status"] == "insufficient_sample"
    assert res["kind"] == "sensor"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_sensor_compare.py -q --tb=short 2>&1 | tail -12`
Expected: FAIL — `KeyError: 'kind'`

- [ ] **Step 3: config 상수를 넣는다**

`ya_config.py` 의 `SENSOR_MIN_SAMPLE` 줄 바로 아래에 추가:

```python
# 센서 판별선 - 이 효과크기 미만이면 근거로 싣지 않는다(후보 목록에는 남는다).
# 도구는 d>0 이면 전부 top-K 에 싣는데, 그대로 리포트 [근거] 로 태우면 d=0.05 짜리가
# 원인 후보로 읽힌다. 0.8 은 Cohen 의 large 관례이며 실데이터를 본 값이 아니다 -
# COMMONALITY_PASS_* 와 같은 성격의 조정 노브다.
SENSOR_PASS_MIN_EFFECT = float(os.getenv("SENSOR_PASS_MIN_EFFECT", "0.8"))
```

- [ ] **Step 4: 센서 도구에 계약을 붙인다**

`tools/sensor_compare.py` 의 `base` 사전에 `"kind": "sensor"` 를 넣는다 (세 반환 경로가 전부 `{**base, ...}` 이므로 한 곳만 고치면 전부 따라온다):

```python
    base = {"kind": "sensor", "candidates": [], "truncated": 0,
            "refetch_key": {"step_seq": step_seq,
                            "target_wafers": targets, "control_wafers": controls,
                            "sensors": [], "store_mode": ya_config.SENSOR_MODE}}
```

후보 생성 루프의 `candidates.append({...})` 를 다음으로 바꾼다:

```python
        candidates.append({
            # 게이트가 조회할 유일한 키. 게이트는 이 문자열을 파싱하지 않는다.
            # step_seq 를 넣는 이유: 다른 스텝의 두 번째 호출이 첫 호출의 근거를
            # 덮어쓰면 안 된다 - 두 스텝은 재실행이 아니라 다른 질문이다.
            "claim_id": f"sensor:{step_seq}:{name}",
            "passes": d >= ya_config.SENSOR_PASS_MIN_EFFECT,
            "reject_reason": (None if d >= ya_config.SENSOR_PASS_MIN_EFFECT
                              else f"효과크기 {round(d, 3)} < "
                                   f"{ya_config.SENSOR_PASS_MIN_EFFECT}"),
            "sensor_name": name,
            "effect_size": round(d, 3),
            "target_mean": round(statistics.mean(t_vals), 3),
            "control_mean": round(statistics.mean(c_vals), 3),
            "target_std": round(statistics.stdev(t_vals), 3),
            "control_std": round(statistics.stdev(c_vals), 3),
            "n_target": len(t_vals), "n_control": len(c_vals),
        })
```

모듈 docstring 의 설계 원칙 목록 끝에 한 줄을 더한다:

```
- **판별선은 효과크기 하나뿐이다.** claim_id 와 passes 를 여기서 발급하는 이유는
  `domain/engine.py` 가 commonality 후보에 하는 것과 같다 - 판정에 필요한 임계를
  아는 곳이 여기이고, `graph/evidence.py` 는 판정하지 않는 자리이기 때문이다.
```

- [ ] **Step 5: 기존 키셋 단언을 갱신한다**

`tests/test_sensor_compare.py::test_return_is_bounded_and_carries_raw_counts` 의 `set(c) == {...}` 를 새 키 3개를 포함하도록 고친다. **키를 지우지 말고 더한다** — 이 단언의 목적은 "wafer 별 원본값을 안 싣는다" 이므로 유계성 검사는 그대로 살아 있어야 한다:

```python
    assert set(c) == {"claim_id", "passes", "reject_reason",
                      "sensor_name", "effect_size", "target_mean", "control_mean",
                      "target_std", "control_std", "n_target", "n_control"}
```

- [ ] **Step 6: 테스트를 통과시킨다**

Run: `python -m pytest tests/test_sensor_compare.py -q --tb=short 2>&1 | tail -8`
Expected: PASS

- [ ] **Step 7: 전체 스위트**

Run: `python -m pytest -q --tb=short 2>&1 | tail -8`
Expected: 444 passed (441 + 새 테스트 3개)

- [ ] **Step 8: 커밋**

```bash
git add ya_config.py tools/sensor_compare.py tests/test_sensor_compare.py
git commit -m "feat(sensor): 2단 센서 후보가 claim_id 와 판별선을 발급한다"
```

---

## Task 2: `build_bundle` 이 센서를 Claim 으로 투영한다

**Files:**
- Modify: `graph/evidence.py:15-48` (Claim), `:50-56` (_FIRST_CLASS_FIELDS), `:259` (Bundle), `:530-533` (_is_hypothesis_result), `:535-619` (build_bundle)
- Test: `tests/test_evidence.py`

**Interfaces:**
- Consumes: Task 1 의 `{"kind": "sensor", candidates: [{claim_id, passes, reject_reason, ...}]}`
- Produces:
  - `Claim.kind: str = "statistical"` (1급 필드)
  - `evidence._is_sensor_result(result) -> bool`
  - `Bundle.statistical_passing() -> list[Claim]` — `passes` 이면서 `kind != "sensor"` 인 claim
  - `build_bundle` 이 센서 finding 을 `claims` 에 넣되 `ran`·`statuses`·`superseded`·`dropped_claims` 에는 **넣지 않는다**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_evidence.py` 상단 픽스처 아래에 헬퍼와 테스트를 추가한다:

```python
def _sensor_finding(step_seq, cands, status="ok", loop=3):
    return {"loop": loop, "tool": "compare_sensor_distribution",
            "args": {"step_seq": step_seq},
            "result": {"kind": "sensor", "status": status, "candidates": cands},
            "thought": "2단"}


def _sensor_cand(step_seq, name, effect, passes=True):
    return {"claim_id": f"sensor:{step_seq}:{name}", "sensor_name": name,
            "effect_size": effect, "passes": passes,
            "reject_reason": None if passes else f"효과크기 {effect} < 0.8",
            "target_mean": 812.4, "control_mean": 799.1,
            "target_std": 3.0, "control_std": 2.8,
            "n_target": 12, "n_control": 40}


def test_sensor_candidate_becomes_a_claim():
    """센서 후보가 claim_id 로 조회된다 - 이것이 이번 변경의 목적이다."""
    b = evidence.build_bundle([_sensor_finding("CC003000",
                                               [_sensor_cand("CC003000", "TEMP_1", 2.31)])])
    c = b.claims["sensor:CC003000:TEMP_1"]
    assert c.kind == "sensor"
    assert (c.step_seq, c.key, c.level) == ("CC003000", "TEMP_1", "sensor")
    assert c.score == 2.31                          # 효과크기가 score 자리에 온다
    assert (c.target_total, c.control_total) == (12, 40)
    assert c.hypothesis_id == ""                    # 등록 가설이 아니다
    assert c.p_permutation is None                  # 설계상 p 를 안 낸다
    assert c.target_wafers == ()                    # 접기 대상이 아니다 (spec §7)
    assert c.extra["target_mean"] == 812.4


def test_sensor_does_not_enter_coverage_vocabulary():
    """센서 status 는 ran/statuses 에 들어오지 않는다.

    들어오면 게이트 (3) 의 `ran_statuses <= NO_DATA_STATUSES` 가 센서의 'ok' 때문에
    영영 False 가 되어 no_comparable_data 가 안 열리고, 센서의 no_signal 이 1단의
    것으로 둔갑해 축을 하나도 안 돌리고 "대조한 축에서는 못 찾았다" 로 끝난다.
    ran/statuses 는 '등록 축 커버리지' 전용 어휘다.
    """
    b = evidence.build_bundle([
        _sensor_finding("CC003000", [], status="no_signal")])
    assert b.ran == set()
    assert b.statuses == {}


def test_second_sensor_call_on_another_step_keeps_the_first():
    """다른 스텝의 센서 호출은 재실행이 아니라 다른 질문이다.

    tool 이름으로 앞 후보를 버리는 폐기 규칙의 근거는 '그룹이 바뀌어 분모가 달라졌다'
    인데, 센서는 group/control 이 주입이라 항상 같고 step_seq 만 다르다. tool 키로
    묶으면 스텝 B 호출이 스텝 A 근거를 조용히 지운다.
    """
    b = evidence.build_bundle([
        _sensor_finding("CC003000", [_sensor_cand("CC003000", "TEMP_1", 2.3)], loop=3),
        _sensor_finding("CD004000", [_sensor_cand("CD004000", "RF_2", 1.9)], loop=4)])
    assert set(b.claims) == {"sensor:CC003000:TEMP_1", "sensor:CD004000:RF_2"}
    assert b.superseded == frozenset()
    assert b.dropped_claims == {}


def test_statistical_passing_excludes_sensors():
    """지목 가능한 후보와 근거로 실리는 후보를 가르는 자리."""
    b = evidence.build_bundle([
        _finding("hyp_eqp_ch_commonality", "eqp_ch_commonality", "ok", [CAND_PASS]),
        _sensor_finding("CC003000", [_sensor_cand("CC003000", "TEMP_1", 2.3)])])
    assert len(b.passing()) == 2                        # 근거로는 둘 다
    assert [c.claim_id for c in b.statistical_passing()] == [CAND_PASS["claim_id"]]


def test_sensor_candidates_do_not_fold_into_one_group():
    """한 호출의 top-K 는 전부 같은 wafer 집합이라, 목록을 실으면 열 개가 한 덩어리로
    접혀 '같은 사실의 열 가지 이름' 이 된다. 온도와 파티클은 다른 설명이다."""
    b = evidence.build_bundle([_sensor_finding("CC003000", [
        _sensor_cand("CC003000", "TEMP_1", 2.3),
        _sensor_cand("CC003000", "RF_2", 1.9)])])
    assert len(b.ranked_groups()) == 2
```

- [ ] **Step 2: 기존 테스트의 계약 변경을 반영한다**

`tests/test_evidence.py::test_sensor_result_is_not_evidence` 는 **의도적으로 뒤집히는 계약**이다. 지우지 말고 이름과 본문을 바꿔, 살아남는 부분(`ran` 제외)을 계속 잠근다:

```python
def test_sensor_result_needs_its_own_discriminator():
    """센서 결과에도 candidates 키가 있다 - 덕타이핑이면 가설 결과와 섞인다.

    이제 센서도 Claim 이 되지만 **판별자는 여전히 다르다**: 가설은 hypothesis_id,
    센서는 kind 다. kind 가 없는 옛 모양은 어느 쪽으로도 안 들어간다 - 정체를 모르는
    결과를 짐작으로 투영하면 필드가 통째로 어긋난다.
    """
    legacy = {"status": "ok", "candidates": [
        {"sensor_name": "rf_power_steady_avg", "effect_size": 14.99, "passes": True}]}
    b = evidence.build_bundle([{"loop": 1, "tool": "compare_sensor_distribution",
                                "args": {}, "result": legacy, "thought": "t"}])
    assert b.claims == {}
    assert b.ran == set()
```

- [ ] **Step 3: 실패를 확인한다**

Run: `python -m pytest tests/test_evidence.py -q --tb=short 2>&1 | tail -12`
Expected: FAIL — `AttributeError: 'Claim' object has no attribute 'kind'`

- [ ] **Step 4: `Claim.kind` 를 1급으로 넣는다**

`graph/evidence.py` 의 `Claim` 에서 `tool` 바로 아래에 추가:

```python
    # 이 후보가 어떤 종류의 증거인가. **게이트 승인 하한이 읽는 자리다.**
    # `_is_statistical` 로 대신하면 안 된다 - 그 함수는 "순열 참조 회차가 0이라 바닥이
    # 1.0" 인 1단 후보에도 False 를 주므로, 그것으로 승인을 막으면 이번 변경이 1단의
    # 승인 가능 범위까지 조용히 좁혀 2026-09-01 에 확정한 순위 계약이 흔들린다.
    kind: str = "statistical"
```

`kind` 는 기본값이 있으므로 기본값 없는 필드들(`claim_id`~`control_total`) **뒤**에 와야 한다. `p_permutation` 바로 위에 놓는다.

`_FIRST_CLASS_FIELDS` 에 `"kind"` 를 더한다.

- [ ] **Step 5: `statistical_passing()` 을 넣는다**

`Bundle.passing()` 바로 아래:

```python
    def statistical_passing(self) -> list[Claim]:
        """**지목 가능한** 통과 후보. `passing()` 은 근거로 실을 것 전부다.

        둘을 가르는 이유: 센서는 근거로 인용되지만 원인 확정의 지목 대상이 아니다
        (다중비교 보정을 일부러 안 한 도구다). 이 구분이 없으면 센서만 통과한 상태에서
        게이트가 승인도 물러섬도 못 하고 루프 한계까지 왕복한다.
        """
        return [c for c in self.passing() if c.kind != "sensor"]
```

- [ ] **Step 6: 판별자와 투영을 넣는다**

`_is_hypothesis_result` 아래에 추가:

```python
def _is_sensor_result(result) -> bool:
    """2단 센서 결과인가. **판별자는 `kind` 다.**

    `candidates` 유무로 가르면 가설 결과와 섞이고, `sensor_name` 같은 후보 내부 키로
    가르면 후보가 0건인 경로(no_signal·insufficient_sample)를 못 알아본다.
    """
    return (isinstance(result, dict)
            and result.get("kind") == "sensor" and "candidates" in result)
```

`build_bundle` 의 루프 앞부분을 바꾼다. **센서는 `ran`/`statuses`/`claims_from` 을 건드리지 않고 곧장 claims 에만 넣고 continue 한다:**

```python
    for i, f in enumerate(findings):
        result = f.get("result")
        if _is_sensor_result(result):
            # **ran / statuses / 폐기 장부에 넣지 않는다.** 그 셋은 '등록 축 커버리지'
            # 전용 어휘라, 센서 status 가 섞이면 게이트 (3) 의 `uncomputable` 이 영영
            # False 가 되고 센서의 no_signal 이 1단의 것으로 둔갑한다.
            # 폐기도 안 건다: claim_id 에 step_seq 가 들어가 같은 스텝 재호출은 같은
            # 키로 덮어써지고, 다른 스텝 호출은 대체가 아니라 다른 질문이다.
            tool = f.get("tool", "")
            for c in result["candidates"]:
                claim_id = c.get("claim_id")
                if not claim_id:
                    continue
                claims[claim_id] = Claim(
                    claim_id=claim_id, tool=tool, kind="sensor",
                    # 등록 가설이 아니다. `_is_roll_up_of` 는 level_columns 가 비어
                    # False 로 떨어지고, `dominates` 의 점수 비교는 같은 값끼리만
                    # 걸리므로 센서끼리만 효과크기로 비교된다.
                    hypothesis_id="",
                    step_seq=str(c.get("step_seq") or f.get("args", {}).get("step_seq") or ""),
                    key=c.get("sensor_name", ""), level="sensor",
                    passes=bool(c.get("passes")),
                    reject_reason=c.get("reject_reason"),
                    score=float(c.get("effect_size") or 0.0),
                    # 2x2 가 아니다. 분모(n)만 참이고 pass 카운트는 없는 값이라
                    # 0 으로 두되 렌더러가 kind 로 막는다.
                    target_pass=0, target_total=int(c.get("n_target") or 0),
                    control_pass=0, control_total=int(c.get("n_control") or 0),
                    # wafer 목록을 안 싣는다: 한 호출의 top-K 는 전부 같은 집합이라
                    # 실으면 서로 다른 센서 열 개가 한 덩어리로 접힌다.
                    extra={k: v for k, v in c.items()
                           if k not in _FIRST_CLASS_FIELDS and k != "sensor_name"},
                )
            continue
        if not _is_hypothesis_result(result):
            if f.get("failed"):
                crashed.add(f.get("tool", ""))
            continue
```

`f.get("args", {}).get("step_seq")` 로 보충하는 이유를 주석으로 남긴다: 센서 후보 dict 에는 `step_seq` 가 없고 claim_id 안에만 있는데, 그것을 파싱하지 않기로 했으므로 감사 기록의 인자에서 읽는다.

- [ ] **Step 7: 테스트를 통과시킨다**

Run: `python -m pytest tests/test_evidence.py -q --tb=short 2>&1 | tail -10`
Expected: PASS

- [ ] **Step 8: 전체 스위트**

Run: `python -m pytest -q --tb=short 2>&1 | tail -10`
Expected: 449 passed. **실패하면 여기서 멈추고 어느 테스트가 왜 깨졌는지 보고한다** — 게이트 쪽이 깨지면 Task 4 가 그것을 고친다는 뜻이므로, 그 테스트 이름을 기록만 하고 넘어간다.

- [ ] **Step 9: 커밋**

```bash
git add graph/evidence.py tests/test_evidence.py
git commit -m "feat(evidence): 센서 결과를 kind='sensor' Claim 으로 투영한다"
```

---

## Task 3: 센서 근거 줄에서 2x2 를 걷어낸다

**Files:**
- Modify: `graph/evidence.py:309-341` (`group_to_dict` 의 `folded`), `:503-528` (`format_evidence_line`)
- Test: `tests/test_evidence.py`

**Interfaces:**
- Consumes: Task 2 의 `Claim.kind`
- Produces: `format_evidence_line(claim_dict)` 가 `kind == "sensor"` 이면 효과크기·n·평균 형식으로 렌더링

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_sensor_evidence_line_never_prints_a_2x2():
    """센서에는 2x2 가 없다. 그대로 태우면 '타깃 0/0 통과' 가 찍히는데, 그것은 숫자가
    없는 것이 아니라 **틀린 숫자**다 - 엔지니어는 대조가 실패한 줄로 읽는다.
    """
    b = evidence.build_bundle([_sensor_finding("CC003000",
                                               [_sensor_cand("CC003000", "TEMP_1", 2.31)])])
    line = evidence.format_evidence_line(asdict(b.claims["sensor:CC003000:TEMP_1"]))
    assert "0/0" not in line
    assert "통과" not in line
    assert "분리 점수" not in line
    assert "효과크기 2.31" in line
    assert "n=12" in line and "n=40" in line
    assert "812.4" in line and "799.1" in line


def test_statistical_evidence_line_is_unchanged():
    """센서 분기를 넣다가 1단 줄을 건드리면 안 된다 (리포트가 이 문장을 쓴다)."""
    b = evidence.build_bundle([_finding("hyp_eqp_ch_commonality", "eqp_ch_commonality",
                                        "ok", [CAND_PASS])])
    line = evidence.format_evidence_line(asdict(b.claims[CAND_PASS["claim_id"]]))
    assert "분리 점수 1.0" in line
    assert "타깃 3/3 통과" in line
    assert "대조군 0/6 통과" in line
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_evidence.py -q --tb=short -k evidence_line 2>&1 | tail -10`
Expected: FAIL — `assert "0/0" not in line`

- [ ] **Step 3: 렌더러를 분기한다**

`format_evidence_line` 의 맨 앞에 넣는다:

```python
    if claim.get("kind") == "sensor":
        # 2x2 도 순열 p 도 없다. 있는 것은 효과크기와 두 분포뿐이고, 그 둘을 표본 수와
        # 함께 읽어야 한다 - 표본이 작으면 효과크기가 커지므로 n 을 떼면 거짓말이 된다.
        ex = claim.get("extra") or {}
        return (f"{claim['claim_id']} · 효과크기 {claim['score']} · "
                f"타깃 n={claim['target_total']} 평균 {ex.get('target_mean')} · "
                f"대조군 n={claim['control_total']} 평균 {ex.get('control_mean')}")
```

`group_to_dict` 의 `folded()` 에도 같은 이유로 분기를 넣는다. 센서는 wafer 목록이 없어 접히지 않지만, 그 규칙이 바뀌면 바로 새는 자리다:

```python
    def folded(c: Claim) -> dict:
        base = {"claim_id": c.claim_id, "hypothesis_id": c.hypothesis_id,
                "kind": c.kind, "level": c.level, "key": c.key,
                "step_seq": c.step_seq, "score": c.score}
        if c.kind == "sensor":
            # 2x2 키를 아예 안 싣는다. `_folded_detail` 은 target_total 유무로
            # 분기하므로, 0 을 실으면 "타깃 0/0" 이 그대로 찍힌다.
            return base
        return {**base, "p_permutation": c.p_permutation,
                "target_pass": c.target_pass, "target_total": c.target_total,
                "control_pass": c.control_pass, "control_total": c.control_total}
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_evidence.py -q --tb=short 2>&1 | tail -8`
Expected: PASS

- [ ] **Step 5: 전체 스위트**

Run: `python -m pytest -q --tb=short 2>&1 | tail -10`

- [ ] **Step 6: 커밋**

```bash
git add graph/evidence.py tests/test_evidence.py
git commit -m "feat(evidence): 센서 근거 줄이 2x2 대신 효과크기와 분포를 적는다"
```

---

## Task 4: 게이트가 센서를 근거로는 싣고 승인 대상에서는 뺀다

**Files:**
- Modify: `graph/nodes.py:381-387` (게이트 (1) 승인 조건), `:417` (게이트 (2)), `:607`, `:620`, `:646` (`_gate_rejection` 의 `passing()`)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: Task 2 의 `Bundle.statistical_passing()`, `Claim.kind`
- Produces: 게이트가 센서 claim 을 지목한 finalize 를 승인하지 않고, 센서만 통과한 상태에서 `no_signal` 로 물러설 수 있다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_graph_nodes.py` 에 픽스처와 테스트를 추가한다:

```python
# 2단 센서 finding. 게이트 계약(claim_id/passes)을 갖지만 등록 축이 아니다.
SENSOR_FINDING = {
    "loop": 3, "tool": "compare_sensor_distribution",
    "args": {"step_seq": "CC002000",
             "group_ids": ["W2406_02", "W2406_04", "W2406_06"],
             "control_ids": ["W2406_01", "W2406_03", "W2406_05"]},
    "result": {"kind": "sensor", "status": "ok", "candidates": [
        {"claim_id": "sensor:CC002000:TEMP_1", "sensor_name": "TEMP_1",
         "effect_size": 2.31, "passes": True, "reject_reason": None,
         "target_mean": 812.4, "control_mean": 799.1,
         "target_std": 3.0, "control_std": 2.8,
         "n_target": 12, "n_control": 40}]},
    "thought": "2단으로 좁힌다",
}


def test_gate_does_not_confirm_on_a_sensor_claim():
    """센서는 인용 가능하되 지목 불가다.

    다중비교 보정을 일부러 안 한 도구(스텝당 센서 수백 개)를 단독 승인 근거로 열면
    1단이 빈손일 때 효과크기 하나로 confirmed 가 나가는 거짓 양성 기계가 된다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "sensor:CC002000:TEMP_1", "hypothesis": "온도", "confidence": 0.95},
        loop=2, update=update, findings=[EQP_CH_SILENT, SENSOR_FINDING])
    assert update.get("finalize_accepted") is None
    assert update.get("finalize_status") != "confirmed"


def test_sensor_only_state_can_still_step_back():
    """**라이브락 방지.** 센서만 통과하면 승인은 막히는데 물러섬까지 막히면 LLM 은
    할 일이 없어 루프 한계까지 왕복하다 inconclusive 로 끝난다 - 근거를 살리려던
    변경이 종료 경로를 막는 것이다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "", "hypothesis": "센서로도 못 좁혔다", "confidence": 0.4},
        loop=2, update=update, findings=[EQP_CH_SILENT, SENSOR_FINDING])
    assert update["finalize_status"] == "no_signal"
    assert update["finalize_accepted"] is True


def test_sensor_evidence_rides_along_in_final_claims():
    """물러서도 센서 근거는 리포트에 실린다 - 그것이 이번 변경의 목적이다."""
    update = {}
    nodes._finalize_gate(
        {"claim_id": "", "hypothesis": "h", "confidence": 0.4},
        loop=2, update=update, findings=[EQP_CH_SILENT, SENSOR_FINDING])
    ids = [c["claim_id"] for c in update["final_claims"]]
    assert "sensor:CC002000:TEMP_1" in ids


def test_statistical_claim_outranks_a_sensor():
    """1단 통과 후보가 있으면 센서는 2등 이하다 (2026-09-01 확정한 등급 계약)."""
    update = {}
    nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:ETCH9_B",
         "hypothesis": "h", "confidence": 0.9},
        loop=2, update=update, findings=[EVIDENCE_FINDING_NEW, SENSOR_FINDING])
    assert update["finalize_status"] == "confirmed"
    ranks = {c["claim_id"]: c["rank"] for c in update["final_claims"]}
    assert ranks["eqp_ch_commonality:chamber:CC002000:ETCH9_B"] == 1
    assert ranks["sensor:CC002000:TEMP_1"] > 1


def test_gate_does_not_offer_sensors_as_pickable_candidates():
    """지목 불가한 것을 '통과 후보' 로 안내하면 LLM 이 골라 제출하고 또 반려당한다."""
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "지어낸:claim:id", "hypothesis": "h", "confidence": 0.9},
        loop=2, update=update, findings=[EQP_CH_SILENT, SENSOR_FINDING])
    assert "sensor:CC002000:TEMP_1" not in verdict
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -q --tb=short -k sensor 2>&1 | tail -15`
Expected: FAIL — `test_gate_does_not_confirm_on_a_sensor_claim` 이 confirmed 를 받는다

- [ ] **Step 3: 게이트 (1) 에 `kind` 하한을 건다**

`graph/nodes.py` 의 승인 조건을 바꾼다:

```python
    # (1) 승인
    if (claim is not None and claim.passes
            # **센서는 지목 대상이 아니다.** 근거로는 아래 _record_evidence 가 함께
            # 싣지만, 다중비교 보정을 일부러 안 한 도구(스텝당 센서 수백 개)를 단독
            # 승인 근거로 열면 1단이 빈손일 때 효과크기 하나로 confirmed 가 나간다.
            # `_is_statistical` 로 대신 걸면 참조 회차 0인 1단 후보까지 함께 막혀
            # 순위 계약이 흔들린다 - 그래서 kind 로 명시한다.
            and claim.kind != "sensor"
            and picked_rank == 1
            and conf >= ya_config.CONFIDENCE_THRESHOLD):
```

판정 순서를 적은 docstring 의 (1)번 줄도 함께 고친다:

```
      (1) 지목한 claim 이 통과 + **가설 도구 발급** + 1등 묶음 + 확신도 충족 -> confirmed
          (2단 센서 claim 은 근거로 실리되 지목 대상이 아니다.)
```

- [ ] **Step 4: `passing()` 네 자리를 옮긴다**

각 자리를 `bundle.statistical_passing()` 으로 바꾼다.

`:417` 게이트 (2):

```python
    # 하한이 `statistical_passing()` 인 이유: 센서가 통과했다고 물러설 길을 닫으면
    # 승인(kind 하한)도 물러섬도 막혀 루프 한계까지 왕복한다. 물러섬의 뜻은 "지목할
    # 원인 후보가 없다" 이지 "아무 근거도 없다" 가 아니다.
    if (not bundle.statistical_passing() and not claim_id
            and "no_signal" in bundle.statuses.values()):
```

`:607`, `:646` 의 `valid = sorted(c.claim_id for c in bundle.passing())` 두 줄과 `:620` 의 `if not bundle.passing():` 을 각각 `statistical_passing()` 으로 바꾼다. `:607` 위 주석("안내 대상은 통과 후보뿐이다")에 한 줄을 덧붙인다:

```python
        # 센서도 뺀다 - 지목할 수 없는 것을 목록에 넣으면 LLM 이 골라 제출하고
        # 또 반려당하는 왕복이 생긴다.
```

`evidence.py` 의 `ranked_groups()` 는 **그대로 둔다.** 센서를 근거 목록에 싣는 것이 이번 변경의 목적이다.

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest tests/test_graph_nodes.py -q --tb=short -k sensor 2>&1 | tail -10`
Expected: PASS (`test_gate_does_not_offer_sensors_as_pickable_candidates` 제외 — Task 5 가 아니라 여기서 통과해야 한다. 실패하면 Step 4 의 `:607` 을 놓친 것이다)

- [ ] **Step 6: 전체 스위트**

Run: `python -m pytest -q --tb=short 2>&1 | tail -10`
Expected: 전부 통과. Task 2 Step 8 에서 기록해 둔 실패가 있었다면 여기서 사라져야 한다.

- [ ] **Step 7: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "feat(gate): 센서를 근거로 싣되 승인 지목 대상에서 뺀다"
```

---

## Task 5: 센서를 지목한 반려가 거짓말하지 않는다

**Files:**
- Modify: `graph/nodes.py:615-650` (`_gate_rejection` 의 `claim is not None` 블록)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: Task 4 의 게이트 `kind` 하한
- Produces: 센서 claim_id 를 지목한 finalize 가 센서 전용 반려 문구를 받는다

**배경 (spec §6b):** Task 4 만 하면 반려 문구가 두 자리에서 거짓을 말한다.
1단 후보가 있으면 순위 반려로 떨어져 `"p None ... p 는 자기 바닥과 함께 읽어야 한다"`
가 나가고(센서는 p 를 애초에 안 낸다), 1단 후보가 없으면 확신도 반려로 떨어져
`"반려: 확신도 0.95 < 0.7"` 이 나간다(넘었는데도).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_sensor_rejection_does_not_cite_p_or_confidence():
    """센서 반려는 센서의 말로 해야 한다.

    순위 반려로 흘리면 'p None' 과 '바닥과 함께 읽어라' 가 나가는데 센서는 p 를 안
    낸다 - 실행 불가능한 지시다. 확신도 반려로 흘리면 임계를 넘은 값을 두고
    '확신도 0.95 < 0.7' 이라고 적는 자기모순이 나간다.
    """
    # 1단 후보가 있어 센서가 2등인 경우
    v1 = nodes._finalize_gate(
        {"claim_id": "sensor:CC002000:TEMP_1", "hypothesis": "h", "confidence": 0.95},
        loop=2, update={}, findings=[EVIDENCE_FINDING_NEW, SENSOR_FINDING])
    # 1단 후보가 없어 센서가 1등인 경우
    v2 = nodes._finalize_gate(
        {"claim_id": "sensor:CC002000:TEMP_1", "hypothesis": "h", "confidence": 0.95},
        loop=2, update={}, findings=[EQP_CH_SILENT, SENSOR_FINDING])

    for v in (v1, v2):
        assert "2단 센서" in v
        assert "hyp_" in v                       # 무엇을 지목해야 하는지 말한다
        assert "바닥" not in v                    # 센서에 없는 개념
        assert "확신도" not in v                  # 넘었는데 모자라다고 말하면 안 된다


def test_sensor_rejection_offers_a_pickable_candidate_when_one_exists():
    """지목할 1단 후보가 있으면 그 이름을 댄다 - 없으면 다음 행동을 안내한다."""
    v = nodes._finalize_gate(
        {"claim_id": "sensor:CC002000:TEMP_1", "hypothesis": "h", "confidence": 0.95},
        loop=2, update={}, findings=[EVIDENCE_FINDING_NEW, SENSOR_FINDING])
    assert "eqp_ch_commonality:chamber:CC002000:ETCH9_B" in v


def test_sensor_rejection_falls_back_to_the_step_back_advice():
    """지목할 1단 후보가 없으면 목록 대신 물러설 길을 안내한다 - 실행 불가능한
    지시를 남기면 루프 한계까지 왕복한다(607·620 자리와 같은 이유)."""
    v = nodes._finalize_gate(
        {"claim_id": "sensor:CC002000:TEMP_1", "hypothesis": "h", "confidence": 0.95},
        loop=2, update={}, findings=[EQP_CH_SILENT, SENSOR_FINDING])
    assert "claim_id 를 비우고 finalize" in v
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/test_graph_nodes.py -q --tb=short -k sensor_rejection 2>&1 | tail -15`
Expected: FAIL — `assert "2단 센서" in v`

- [ ] **Step 3: 분기를 넣는다**

`_gate_rejection` 의 `if claim is not None:` 블록에서 `if not claim.passes:` 처리 **바로 다음, 순위 비교 앞**에 넣는다:

```python
        if claim.kind == "sensor":
            # **센서의 말로 반려한다.** 아래 순위 반려는 p 와 바닥을 인용하는데 센서는
            # p 를 안 내므로 'p None' 과 실행 불가능한 지시가 나가고, 그 아래 확신도
            # 반려는 임계를 넘은 값을 두고 모자라다고 적는 자기모순을 낸다.
            valid = sorted(c.claim_id for c in bundle.statistical_passing())
            nxt = (f" 지목 가능한 후보: {', '.join(valid)}." if valid
                   else f" {_no_candidate_action(bundle, coverage)}")
            return (f"반려: {claim.claim_id} 는 2단 센서 후보다. 센서 근거는 리포트에 "
                    f"함께 실리지만 **원인 확정의 지목 대상이 아니다** - 스텝당 센서가 "
                    f"수백 개라 효과크기 순위만으로는 우연한 분리를 가릴 수 없다. "
                    f"가설 도구(hyp_*)가 발급한 claim_id 를 지목하라.{nxt}")
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_graph_nodes.py -q --tb=short -k sensor 2>&1 | tail -10`
Expected: PASS

- [ ] **Step 5: 전체 스위트**

Run: `python -m pytest -q --tb=short 2>&1 | tail -10`

- [ ] **Step 6: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "fix(gate): 센서 지목 반려가 p 와 확신도로 거짓말하지 않게 한다"
```

---

## Task 6: LLM 계약에 센서의 지위를 적는다

**Files:**
- Modify: `domain/hypotheses.yaml` — 4가설의 `description:` 블록 (통계 해석 문단이 여기 있다)
- Modify: `llm/client.py` (운영 sys 프롬프트)
- Modify: `tools/agent_tools.py:56-62` (`finalize` docstring)
- Test: `tests/test_registry.py`

**Interfaces:**
- Consumes: Task 1~5 의 동작
- Produces: 코드 변경 없음. LLM 이 읽는 문서 3종이 코드와 같은 말을 한다

**왜 필수인가:** `hypotheses.yaml` 은 코드가 아니라 **LLM 이 읽는 계약 문서**다. 지난 순열 p 교정 때 리뷰 Important 4건 중 2건이 바로 이 계약 구멍이었다. 코드만 고치고 yaml 을 두면 계약이 거짓이 된다.

- [ ] **Step 1: 지금 무엇이 적혀 있는지 읽는다**

Run: `grep -n "claim_id" domain/hypotheses.yaml llm/client.py tools/agent_tools.py`

통계 해석 문단은 각 가설의 `description: |` 블록 안에 있고 **4가설에 복붙돼 있다.**
`load_hypotheses()` 는 그것을 `spec["description"]` 으로 싣고, `registry.build_tools` 가
StructuredTool 의 docstring 으로 넘겨 LLM 이 읽는다. **네 곳 전부** 고쳐야 한다.

- [ ] **Step 2: 계약 테스트를 쓴다**

기존 yaml 계약 테스트 파일(`tests/test_registry.py`)에 추가:

```python
def test_every_hypothesis_says_sensor_claims_are_not_pickable():
    """yaml 은 LLM 이 읽는 계약 문서다. 규칙의 반쪽만 적으면 거짓이 된다.

    센서 claim_id 가 생겼으므로 LLM 은 그것을 finalize 에 낼 수 있다고 읽는다.
    네 가설 중 하나라도 빠지면 그 축을 돌린 대화에서만 계약이 사라진다.
    """
    from domain import registry
    specs = registry.load_hypotheses()
    assert len(specs) >= 4
    for spec in specs:
        desc = spec["description"]
        assert "2단 센서" in desc, spec["id"]
        assert "지목 대상이 아니다" in desc, spec["id"]
```

- [ ] **Step 3: 실패를 확인한다**

Run: `python -m pytest tests/test_registry.py -q --tb=short -k sensor 2>&1 | tail -8`
Expected: FAIL

- [ ] **Step 4: yaml 4곳에 문장을 넣는다**

각 가설의 `description: |` 블록 **끝에** 같은 문장을 넣는다 (들여쓰기 4칸을 맞춘다):

```
    2단 센서(compare_sensor_distribution) 후보에도 claim_id 가 실린다. 그것은
    리포트에서 근거로 인용하기 위한 것이며 finalize 의 지목 대상이 아니다 -
    스텝당 센서가 수백 개라 효과크기 순위만으로는 우연한 분리를 가릴 수 없다.
    원인을 확정할 때는 가설 도구(hyp_*)가 발급한 claim_id 를 지목하라.
```

- [ ] **Step 5: 운영 프롬프트와 finalize docstring 에 같은 문장을 넣는다**

`llm/client.py` 의 운영 sys 프롬프트(분석 루프용)에 한 줄:

```
- 2단 센서 후보의 claim_id 는 근거 인용용이다. finalize 로 지목할 수 있는 것은
  가설 도구(hyp_*)가 발급한 claim_id 뿐이다.
```

`tools/agent_tools.py::finalize` 의 `claim_id:` 설명에 한 줄:

```
      2단 센서(sensor:...) 의 claim_id 는 지목 대상이 아니다 - 반려된다.
```

- [ ] **Step 6: 통과 확인 + 전체 스위트**

Run: `python -m pytest -q --tb=short 2>&1 | tail -10`

- [ ] **Step 7: 커밋**

```bash
git add domain/hypotheses.yaml llm/client.py tools/agent_tools.py tests/test_registry.py
git commit -m "docs(contract): LLM 계약이 센서 claim_id 의 지위를 말하게 한다"
```

---

## Task 7: 훼손 실험 — 잠근 것이 실제로 잠겼는지 본다

**Files:** 없음 (검증만). 훼손은 반드시 되돌린다.

**Interfaces:**
- Consumes: Task 1~6 전부
- Produces: 훼손 목록과 각각의 결과(잡힘/안 잡힘). 안 잡힌 것마다 테스트를 추가한다.

**왜 필수인가:** 지난 5회 리뷰에서 매번 실재 결함이 나왔고 그 절반이 "고치다 만든 회귀" 였다. 훼손을 안 하면 테스트가 있어도 매번 3건씩 안 잡힌다.

> ⚠️ **함정 2건 (과거에 실제로 당했다).**
> 1. 훼손 루프가 도구 타임아웃에 죽으면 훼손 파일이 다음 실행의 기준선이 된다.
>    **끝나면 반드시 `git diff` 가 비었는지 확인한다.**
> 2. `git checkout` 으로 훼손을 되돌리면 커밋 안 된 작업까지 날아간다. Task 1~6 이
>    전부 커밋된 상태에서만 시작한다.

- [ ] **Step 1: 시작 전 상태를 확인한다**

Run: `git status --short` → 비어 있어야 한다. 비어 있지 않으면 멈추고 보고한다.

- [ ] **Step 2: 훼손을 하나씩 넣고 스위트를 돌린다**

각 훼손마다: 고친다 → `python -m pytest -q --tb=no 2>&1 | tail -3` → 결과 기록 → `git checkout -- <file>` 로 되돌린다.

| # | 파일 | 훼손 | 잡아야 하는 이유 (spec) |
|---|---|---|---|
| 1 | `sensor_compare.py` | `passes` 를 항상 `True` 로 | §8 판별선 |
| 2 | `sensor_compare.py` | claim_id 에서 `step_seq` 를 뺀다 | §5 폐기 — 두 스텝이 충돌한다 |
| 3 | `sensor_compare.py` | `base` 에서 `"kind"` 를 뺀다 | Task 1 판별자 |
| 4 | `evidence.py` | 센서 분기 **안에** `ran.add(tool)` 과 `statuses[tool] = result.get("status", "")` 를 넣는다 | **§5 회귀 3건.** ⚠️ `continue` 를 지우는 훼손은 **동작을 안 바꾼다** — 바로 아래 `_is_hypothesis_result` 가 False 라 그쪽 `continue` 로 빠진다(Task 2 리뷰 지적) |
| 5 | `evidence.py` | 센서 Claim 에 `target_wafers` 를 실어 준다 | §7 열 개가 한 덩어리로 접힌다 |
| 6 | `evidence.py` | `statistical_passing()` 이 `passing()` 을 그대로 돌려주게 | **§6 라이브락** |
| 7 | `evidence.py` | `format_evidence_line` 의 센서 분기를 지운다 | §9 "타깃 0/0 통과" |
| 8 | `nodes.py` | 게이트 (1) 의 `claim.kind != "sensor"` 를 지운다 | §2 센서 단독 승인 |
| 9 | `nodes.py` | (2) 의 `statistical_passing()` 을 `passing()` 으로 되돌린다 | **§6 라이브락** |
| 10 | `nodes.py` | `:607` 의 `statistical_passing()` 을 `passing()` 으로 | 지목 불가 후보 안내 |
| 11 | `nodes.py` | `_gate_rejection` 의 센서 분기를 지운다 | §6b 거짓말 2건 |
| 12 | `hypotheses.yaml` | 가설 **하나**에서만 새 문장을 뺀다 | Task 6 — 4곳 동기 |
| 13 | `nodes.py` | `if not bundle.statistical_passing():`(미통과 지목의 물러설 길)를 `passing()` 으로 | **§6 라이브락** — 리뷰가 찾은 누락 |
| 14 | `nodes.py` | claim_id 미제출 안내의 `valid` 를 `passing()` 으로 | 지목 불가 후보 안내 — 리뷰가 찾은 누락 |
| 15 | `llm/client.py` | `_top_ranked` 를 `groups[0].lead` 로 되돌린다 | 각본이 센서를 지목해 반려 왕복 |
| 16 | `hypotheses.yaml` | 판별선 절만 뺀다(4곳 전부) | 계약의 반쪽이 사라진다 |

> **규칙: 한 규칙을 N 자리에 옮겼으면 N 자리를 전부 훼손한다.** 이번에 설계서 §6 표는
> `passing()` 자리 넷을 명시했는데 훼손 표는 셋만 다뤄, 나머지 둘(`13`·`14`)이 되돌려도
> 스위트가 초록인 채로 남아 있었다 — 리뷰가 그것을 찾았다.

- [ ] **Step 3: 안 잡힌 훼손마다 테스트를 추가한다**

훼손을 넣었는데 스위트가 초록이면 **그 자리는 잠겨 있지 않다.** 실패하는 테스트를 먼저 쓰고(훼손 상태에서 빨강), 훼손을 되돌리고(초록), 커밋한다.

특히 4번·6번·9번은 이 계획의 핵심 안전판이므로, 안 잡히면 반드시 테스트를 추가한다.

- [ ] **Step 4: 워킹 트리가 깨끗한지 확인한다**

Run: `git status --short && git diff --stat`
Expected: 둘 다 비어 있다. **비어 있지 않으면 훼손이 남은 것이다 — 되돌린다.**

- [ ] **Step 5: 최종 스위트**

Run: `python -m pytest -q --tb=short 2>&1 | tail -8`
Expected: 전부 통과. 최종 개수를 기록한다 (새 기준선이 된다).

- [ ] **Step 6: 실제 실행으로 확인한다**

Run: `python main.py 2>&1 | tail -30`
Expected: 예외 없이 리포트가 나온다. 센서 근거가 실린 경우 `[근거]` 줄에 `타깃 0/0` 이 없어야 한다.

- [ ] **Step 7: 훼손 결과를 커밋한다 (테스트를 추가했다면)**

```bash
git add tests/
git commit -m "test(sensor): 훼손 실험에서 안 잡힌 자리를 잠근다"
```

---

## 완료 후

1. `superpowers:requesting-code-review` 로 리뷰를 받는다 (구현 Sonnet 5 / 리뷰 Opus 5).
2. 리뷰 지적을 반영한 뒤 `superpowers:finishing-a-development-branch` 로 main 에 ff 병합한다.
3. 메모리(`cross-axis-rank-branch-in-flight.md`)를 갱신한다: 새 기준선 커밋·테스트 수, 확정 계약, ⏭ 다음 = **B (판별선의 이진 붕괴 완화)**.
