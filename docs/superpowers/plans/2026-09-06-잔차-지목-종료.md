# 잔차 지목 종료 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `(2a) weak_signal` 의 하한 `not claim_id` 를 "정직한 제출" 로 넓혀, LLM 이 잔차나 통과 센서를 지목해도 반려 왕복 없이 물러설 수 있게 한다.

**Architecture:** 게이트 조건 한 줄과 판정문 한 절, 프롬프트 두 곳만 바꾼다. `not bundle.statistical_passing()` 하한은 **그대로 남겨** `_fold_key` 접기 계약을 건드리지 않는다. 새 노브도 새 계산도 만들지 않는다.

**Tech Stack:** Python 3, pytest, LangGraph. 콘솔은 cp949 이므로 실행은 `PYTHONIOENCODING=utf-8` 을 붙인다.

**Spec:** `docs/superpowers/specs/2026-09-06-잔차-지목-종료-design.md`

## Global Constraints

- 기준선: main `64da0a9` / **496 passed**. 브랜치 `feat/residual-pick-exit`.
- 커밋 트레일러는 매 커밋에 붙인다:
  ```
  Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01PRcnPzkP8n4MVEC7kRT64u
  ```
- 테스트 실행: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short`
- **훼손 되돌리기에 `git checkout` 금지.** 원본을 메모리에 보관하고 `finally` 로 복원한다. 파일이 CRLF 이므로 읽기·쓰기 둘 다 `newline=""` 으로 연다. 이 저장소에서 커밋 안 된 작업을 두 번 날렸다.
- **테스트는 이름이 주장하는 조건을 잠근다.** 다른 조건을 전부 만족시킨 상태에서 그 하나만 걸리게 조립한다. 이 저장소는 "이름과 다른 이유로 통과하는 테스트" 에 여섯 번 당했다.
- **프롬프트 단언은 반드시 `grep` 으로 먼저 확인한다.** 단언할 문자열이 신규 문장에만 있는지 보고 나서 테스트를 쓴다. 옆 문장에 이미 있던 문자열을 잡아 공허해진 단언이 두 번 나왔다.
- `graph/nodes.py:528`((4) inconclusive)·`:817`(게이트 미경유 백스톱)·`graph/evidence.py::_fold_key` 는 **건드리지 않는다**(설계 §9).

---

### Task 1: 게이트 하한을 넓히고 판정문이 지목을 말하게 한다

**Files:**
- Modify: `graph/nodes.py` — `_finalize_gate` 의 `(2a)` 블록 (현재 412-436) + 함수 docstring 의 판정 순서 목록 (354)
- Test: `tests/test_graph_nodes.py`

**Interfaces:**
- Consumes: `bundle.claims`(dict, 번들 **전체** 조회) · `bundle.statistical_passing()` · `bundle.residuals()` · `claim` 지역변수(게이트 상단 `claim = bundle.claims.get(claim_id)`)
- Produces: `(2a)` 가 잔차 지목·센서 지목에서도 열린다. Task 2 의 프롬프트 문장이 이 동작을 설명한다.

- [ ] **Step 1: 실패하는 테스트 3건을 먼저 쓴다**

`tests/test_graph_nodes.py` 의 `test_a_weak_only_state_ends_as_weak_signal` **바로 뒤**에 넣는다. 픽스처 `EQP_CH_BELOW_LINE`(:2650)·`SENSOR_FINDING`(:119)은 이미 있다.

```python
def test_a_named_residual_still_ends_as_weak_signal():
    """지목했다는 이유만으로 잔차를 태우지 않는다.

    (2a)의 하한이 `not claim_id` 이던 동안에는, LLM 이 약한 후보를 지목하는 순간
    이 문이 닫혀 (5) 반려로 갔다. 반려를 되풀이하면 루프 한계에서 inconclusive +
    final_claims=[] 로 끝나 잔차가 소각된다 - 이 기능이 없애려던 바로 그 상태다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:ETCH9_B",
         "hypothesis": "ETCH9_B 편중", "confidence": 0.9},
        loop=2, update=update, findings=[EQP_CH_BELOW_LINE])
    assert update["finalize_status"] == "weak_signal"
    assert update["finalize_accepted"] is True
    ids = [c["claim_id"] for c in update["final_claims"]]
    assert ids == ["eqp_ch_commonality:chamber:CC002000:ETCH9_B"]
    # 지목을 받아준 것이지 원인으로 확정한 것이 아니다 - 판정문이 그렇게 말해야
    # LLM 이 서술에서 그 후보를 단정하지 않는다.
    assert "원인으로 확정하지 않았다" in verdict, verdict
    # 확정하지 않기로 했으므로 picked 표시를 안 붙인다.
    assert not any(c.get("picked_by_llm") for c in update["final_claims"])


def test_a_named_passing_sensor_ends_as_weak_signal():
    """지목할 수 있는 것이 하나도 없는 상태에서 반려는 왕복만 만든다.

    1단이 아무것도 못 갈랐고 2단 센서만 통과한 상태다. 여기서 "센서는 지목 대상이
    아니다" 를 돌려줘도 LLM 이 대신 고를 것이 없다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "sensor:CC002000:TEMP_1",
         "hypothesis": "TEMP_1 이상", "confidence": 0.9},
        loop=2, update=update, findings=[EQP_CH_BELOW_LINE, SENSOR_FINDING])
    assert update["finalize_status"] == "weak_signal"
    # 센서는 판별선을 **넘었다** - "판별선을 넘지 못해" 라고 쓰면 거짓말이다.
    assert "2단 센서라 원인으로 확정하지 않았다" in verdict, verdict
    ids = [c["claim_id"] for c in update["final_claims"]]
    assert "sensor:CC002000:TEMP_1" in ids
    assert "eqp_ch_commonality:chamber:CC002000:ETCH9_B" in ids


def test_a_made_up_claim_id_is_still_rejected_not_absorbed():
    """지어낸 이름은 '이 표본으로는 갈리지 않았다' 와 다른 사실이다.

    문을 claim_id 무관하게 열면 환각이 weak_signal 로 조용히 흡수돼, 엔지니어도
    모르고 다음에 모델을 바꿀 때 품질 저하를 못 본다.
    """
    update = {}
    verdict = nodes._finalize_gate(
        {"claim_id": "eqp_ch_commonality:chamber:CC002000:NOPE",
         "hypothesis": "지어낸 것", "confidence": 0.9},
        loop=2, update=update, findings=[EQP_CH_BELOW_LINE])
    assert "finalize_status" not in update
    assert "finalize_accepted" not in update
    assert "도구 결과에 없다" in verdict, verdict
```

- [ ] **Step 2: 실패를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_graph_nodes.py -k "named_residual or named_passing_sensor or made_up_claim_id" --tb=short`

Expected: 앞의 두 개가 FAIL(현재 하한이 `not claim_id` 라 반려로 간다), 세 번째는 이미 PASS(회귀 잠금용). **두 개가 실패하는 이유가 "반려로 갔다" 인지 출력으로 확인한다** - 다른 이유로 빨강이면 픽스처가 잘못된 것이다.

- [ ] **Step 3: 하한을 넓힌다**

`graph/nodes.py` 의 `(2a)` 조건:

```python
    residuals = bundle.residuals()
    if (not bundle.statistical_passing() and residuals
            and (not claim_id or claim is not None)):
```

블록 주석 끝에 왜 넓혔는지를 덧붙인다:

```python
    #      **하한은 `not claim_id` 가 아니라 "정직한 제출" 이다.** 빈손일 때만 열면
    #      이 문이 LLM 의 협조에만 열린다 - 약한 후보를 지목하는 순간 닫혀 (5) 반려로
    #      가고, 반려를 되풀이하면 루프 한계에서 잔차가 그대로 소각된다(이 기능이
    #      없애려던 상태). `claim is not None` 은 번들 전체 조회라 잔차도 센서도
    #      포함하고, **지어낸 이름만** 걸러 낸다 - 환각은 "이 표본으로는 갈리지
    #      않았다" 와 다른 사실이라 같은 이름을 주면 안 된다.
    #      `not bundle.statistical_passing()` 은 **그대로 둔다.** 이것이 남아 있어야
    #      `passing() + residuals` 의 비센서 claim 이 전부 미통과라 `_fold_key` 가
    #      통과 claim 과 잔차를 한 묶음에 섞지 않는다(설계 §4).
```

- [ ] **Step 4: 판정문에 지목 절을 넣는다**

같은 블록의 `return` 을 바꾼다. **두 갈래로 가른다** - 센서는 판별선을 넘었으므로 "판별선을 넘지 못해" 라고 쓰면 거짓이다. 미통과 센서를 지목한 경우도 같은 문장으로 덮이도록 센서 갈래에는 "근거로는 함께 싣는다" 같은 사족을 붙이지 않는다(미통과 센서는 근거로도 안 실린다).

```python
        if not claim_id:
            picked_note = ""
        elif claim.kind == "sensor":
            picked_note = f"네가 지목한 {claim_id} 는 2단 센서라 원인으로 확정하지 않았다. "
        else:
            picked_note = (f"네가 지목한 {claim_id} 는 판별선을 넘지 못해 "
                           f"원인으로 확정하지 않았다. ")
        return (f"약한 신호 ({_coverage_phrase(coverage)}): {picked_note}"
                f"판별선을 넘은 원인 후보는 없고, 아랫선을 넘은 잔차 {len(residuals)}건을 "
                f"근거로 싣는다. 확정이 아니라 '이 표본으로는 갈리지 않았다' 는 뜻이다. "
                f"리포팅으로 진행한다.")
```

- [ ] **Step 5: 거짓이 된 문구 두 개를 고친다**

(a) `_record_evidence` 호출 위 주석이 지금 이렇게 적혀 있다 — `picked 는 넘기지 않는다(`not claim_id` 가 하한이므로 지목이 없다는 것이 이 분기의 전제다)`. **그 전제가 방금 깨졌다.** 이유를 바꿔 쓴다:

```python
        # picked 는 넘기지 않는다 - 지목이 있을 수 있지만(하한이 "정직한 제출" 로
        # 넓어졌다) 그것을 **원인으로 확정하지 않기로** 했기 때문이다. picked_by_llm
        # 을 붙이면 리포트가 그 후보를 서술의 축으로 삼아 단정하게 된다.
```

(b) `_finalize_gate` docstring 의 판정 순서 목록(:354)이 `(2a) 지목 없이 물러섰는데 ...` 다. `지목 없이` 를 지우고 새 하한을 반영한다:

```
      (2a) 통과 후보 0 + 아랫선을 넘은 잔차 있음 + 제출이 정직함(빈손이거나 실재하는
           claim_id) -> weak_signal
```

- [ ] **Step 6: 통과를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_graph_nodes.py -k "named_residual or named_passing_sensor or made_up_claim_id" --tb=short`
Expected: 3 passed.

- [ ] **Step 7: 전체 스위트를 돌리고 깨진 것을 분류한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short -rf`

**설계 §11 대로 처리한다.** "잔차/센서를 지목하면 반려한다" 를 잠그던 테스트가 깨질 것이다. 각각에 대해:

- 그 테스트가 잠그던 규칙이 **이 변경과 무관하면**(예: "반려 안내가 거짓말을 하지 않는다", "없는 claim_id 와 대체된 claim_id 를 가른다") 후보 점수를 `RESIDUAL_MIN_SCORE`(0.25) 아래로 낮춰 **그 테스트가 쓰인 상태 그대로 살린다.** 잔차가 없으면 `(2a)` 가 안 열리므로 원래 반려 경로를 그대로 탄다.
- 규칙 자체가 뒤집히는 것만(예: "잔차를 지목하면 반려된다") 새 계약을 단언하도록 고친다.

**어느 쪽으로 처리했는지 테스트마다 한 줄 주석으로 남긴다.** 판단이 갈리면 보고서에 적고 진행한다.

- [ ] **Step 8: 커밋**

```bash
git add graph/nodes.py tests/test_graph_nodes.py
git commit -m "feat(gate): 잔차를 지목해도 weak_signal 로 물러설 수 있게 한다"
```

---

### Task 2: LLM 계약 두 곳을 새 동작에 맞춘다

**Files:**
- Modify: `llm/client.py` — 운영 리포트 sys 프롬프트 `weak_signal` 절 (419-421)
- Modify: `graph/nodes.py` — `ANALYZE_SYSTEM_PROMPT` 의 47·48번 줄
- Modify: `docs/superpowers/specs/2026-09-04-잔차-출력-design.md` — §12 성공 기준 3
- Test: `tests/test_mock_llm.py`

**Interfaces:**
- Consumes: Task 1 이 만든 동작(잔차·센서 지목에서 `(2a)` 가 열린다)
- Produces: 없음 (계약 문서화가 산출물이다)

- [ ] **Step 1: 먼저 grep 으로 무엇이 이미 있는지 확인한다**

Run:
```bash
grep -n "원인으로 단정하지" llm/client.py
grep -n "확정 결론을 쓰지 마라" llm/client.py
```

**중요:** 운영 sys 프롬프트의 `weak_signal` 절은 이미 `"후보는 나왔으나 판별선을 넘지 못한 것이니 원인으로 단정하지 말고"` 와 `"확정 결론을 쓰지 마라"` 를 담고 있다. **그 문자열로 단언하면 공허해진다** - 새 문장을 지워도 통과한다.

새로 필요한 사실은 다른 것이다: **제출된 `hypothesis` 자체가 특정 후보를 원인으로 지목하고 있을 수 있다**(하한이 넓어져 지목한 제출도 이 판정으로 온다). 리포트 작성자가 그 문장을 그대로 옮기면 판정과 서술이 어긋난다. 단언은 이 사실을 말하는 **신규 문자열**에만 건다.

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`tests/test_mock_llm.py` 의 `test_operational_prompt_tells_the_report_what_weak_signal_means` 바로 뒤에 넣는다. 그 테스트가 쓰는 헬퍼(`ScriptedMockLLMClient` / `_openai_client()` / `seen_sys`)를 그대로 따른다.

```python
def test_operational_prompt_says_a_submitted_hypothesis_may_name_a_weak_candidate():
    """하한이 넓어져 '지목한 제출'도 weak_signal 로 온다.

    그 제출의 hypothesis 는 후보 하나를 원인으로 지목하는 문장이다. 리포트
    작성자가 그것을 그대로 옮기면 판정("확정이 아니다")과 서술("X가 원인")이
    한 리포트 안에서 어긋난다. 기존 절은 **claims 목록**을 단정하지 말라고만
    했지 **제출된 가설 문장**을 어떻게 다루라고는 말하지 않는다.
    """
    client = _openai_client()
    client.generate_report(
        hypothesis="ETCH9_B 편중이 원인", confidence=0.9,
        finalize_status="weak_signal", claims=[], coverage={}, findings=[])
    sys = client.seen_sys
    assert "제출된 가설이 특정 후보를 원인으로 지목하고 있어도" in sys, sys
```

- [ ] **Step 3: 실패를 확인하고, 단언 문자열이 신규인지 grep 으로 증명한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_mock_llm.py -k "submitted_hypothesis_may_name" --tb=short`
Expected: FAIL

Run: `grep -c "제출된 가설이 특정 후보를 원인으로 지목하고 있어도" llm/client.py`
Expected: **0** (아직 없으므로 이 단언은 공허하지 않다)

- [ ] **Step 4: 운영 sys 프롬프트에 한 문장을 더한다**

`llm/client.py` 의 `weak_signal` 절(421번 줄 `"...확정 결론을 쓰지 마라. "` 바로 뒤)에 이어 붙인다:

```python
            "제출된 가설이 특정 후보를 원인으로 지목하고 있어도 그 문장을 그대로 "
            "옮기지 마라 - 게이트는 그 후보를 원인으로 확정하지 않았다. "
```

- [ ] **Step 5: 분석 프롬프트에서 거짓이 된 두 문장을 고친다**

`graph/nodes.py` 의 `ANALYZE_SYSTEM_PROMPT`. **둘 다 "반려된다" 를 단언하는데, 새 동작에서는 그 상태가 반려되지 않는다.**

(a) 48번 줄 끝 `- 억지로 지목하면 반려되고, 같은 반려를 되풀이하면 루프 예산만 태운다.` 를 바꾼다:

```
- 억지로 지목해도 게이트는 그 후보를 원인으로 확정하지 않고 잔차로 실은 채 끝낸다. 물러서는 쪽이 낫다 - 빈손으로 내면 약한 후보를 원인으로 단언하는 문장을 애초에 쓰지 않게 된다.
```

(b) 47번 줄의 `센서는 ... 우연한 분리를 가릴 수 없어 반려된다.` 는 **조건부로만 참이다**(통과한 1단 후보가 있으면 반려되고, 하나도 없으면 이제 weak_signal 로 끝난다). 다음으로 바꾼다:

```
센서는 스텝당 수백 개라 다중비교 보정 없이 효과크기 순위만으로는 우연한 분리를 가릴 수 없어, 지목해도 원인으로 확정되지 않는다.
```

- [ ] **Step 6: B 설계서의 성공 기준 3 을 고친다**

`docs/superpowers/specs/2026-09-04-잔차-출력-design.md` §12 의 기준 3 이 지금 `잔차는 finalize 로 지목할 수 없고, 지목하면 그 사유로 반려된다.` 다. **뒤 절이 거짓이 된다.** 바꾼다:

```
3. 잔차는 `finalize` 로 지목해도 원인으로 확정되지 않는다. 다만 반려로 왕복시키지는
   않는다 - 후속 `2026-09-06-잔차-지목-종료-design.md` 가 `(2a)` 하한을 "정직한
   제출" 로 넓혔다.
```

- [ ] **Step 7: 통과를 확인하고 전체를 돌린다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short -rf`
Expected: 전부 통과. 분석 프롬프트 문장을 바꿨으므로 그 문장을 단언하던 테스트가 있으면 함께 고친다(고쳤으면 보고서에 적는다).

- [ ] **Step 8: 커밋**

```bash
git add llm/client.py graph/nodes.py tests/test_mock_llm.py docs/superpowers/specs/2026-09-04-잔차-출력-design.md
git commit -m "docs(contract): 지목한 제출도 물러설 수 있다는 것을 계약 두 곳이 말하게 한다"
```

---

### Task 3: 훼손 실험과 최종 검증

**Files:** 없음 (검증만). 훼손은 반드시 되돌린다.

**Interfaces:**
- Consumes: Task 1·2 전부
- Produces: 훼손 결과표. 안 잡힌 자리마다 테스트를 추가한다.

> ⚠️ **되돌리는 방법.** `git checkout -- <파일>` 을 쓰지 않는다. 원본을 메모리에 보관했다가 `finally` 로 복원하는 드라이버를 쓰고, 배치마다 `git status --short` 로 확인한다. 파일이 CRLF 이므로 읽기·쓰기 둘 다 `newline=""` 으로 연다. 드라이버는 저장소가 아니라 세션 scratchpad 에 둔다.

- [ ] **Step 1: 시작 전 상태를 확인한다**

Run: `git status --short`
Expected: 미추적 문서 2개(`docs/2026-08-*.md`) 외에 없다. 아니면 멈추고 보고한다.

- [ ] **Step 2: 훼손을 하나씩 넣고 스위트를 돌린다**

각 훼손마다: 고친다 → `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=no -rf` → 결과 기록 → 복원.

| # | 파일 | 훼손 | 잡아야 하는 이유 |
|---|---|---|---|
| 1 | `nodes.py` | 새 하한에서 `not bundle.statistical_passing()` 제거 | 통과 후보가 있는데 물러섬으로 끝난다 |
| 2 | `nodes.py` | 새 하한에서 `and residuals` 제거 | 실을 것이 없는데 약한 신호라고 한다 |
| 3 | `nodes.py` | 새 하한을 `not claim_id` 로 되돌린다 | 이 브랜치가 통째로 사라진다 |
| 4 | `nodes.py` | 새 하한을 `claim is not None` **만**으로 (빈손을 뺀다) | 물러선 제출이 다시 반려된다 |
| 5 | `nodes.py` | 새 하한에서 `or claim is not None` 대신 무조건 참 | **환각이 조용히 흡수된다** |
| 6 | `nodes.py` | `picked_note` 를 빈 문자열 고정 | LLM 이 지목이 채택된 줄 안다 |
| 7 | `nodes.py` | `picked_note` 의 센서 갈래를 미통과 갈래와 합친다 | 통과한 센서에 "판별선을 넘지 못해" 라고 거짓말한다 |
| 8 | `nodes.py` | `(2a)` 의 `_record_evidence` 에 `None` 대신 `picked` 를 넘긴다 | 확정 안 하기로 한 후보에 `picked_by_llm` 이 붙는다. ⚠️ **픽스처를 통과 센서 지목으로 잡아야 유의미하다** - `picked` 는 `ranked_groups()`(통과 후보만)에서 찾으므로 잔차 지목이면 원래 `None` 이라 이 훼손이 아무것도 안 바꾼다 |
| 9 | `nodes.py` | `(2a)` 블록을 `(2)` 뒤로 옮긴다 | B 의 순서 계약이 살아 있는지 재확인 |
| 10 | `client.py` | 운영 sys 프롬프트의 새 문장 제거 | 서술이 약한 후보를 단정한다 |
| 11 | `nodes.py` | `ANALYZE_SYSTEM_PROMPT` 48번 줄의 고친 문장 제거 | 계약의 반쪽이 사라진다 |
| 12 | `evidence.py` | **`_fold_key` 가 `passes` 를 보게 한다** (`return (0 if claim.passes else 1, *_order_key(claim), ...)`) | **아무것도 안 깨져야 한다.** 방향이 반대인 훼손이다 - 설계 §4 의 "통과 claim 과 잔차가 한 묶음에 못 들어온다" 를 코드로 확인한다. 깨지면 §4 의 전제가 틀린 것이고 이 브랜치의 근거가 무너지므로 **즉시 멈추고 보고한다** |

- [ ] **Step 3: 안 잡힌 훼손마다 테스트를 추가한다**

훼손을 넣었는데 초록이면 그 자리는 잠겨 있지 않다. 실패하는 테스트를 먼저 쓰고(훼손 상태에서 빨강), 훼손을 되돌리고(초록), 커밋한다. **1·5·7·8 은 이 계획의 핵심 안전판**이므로 안 잡히면 반드시 추가한다.

> **훼손 표는 구현 전에 쓰였다.** B 브랜치에서 16행을 전부 돌려 18/18 CAUGHT 를 받고도, 최종 리뷰가 브랜치 diff 를 걸어 보니 훼손이 아예 없는 자리가 있었고 그중 둘이 안 잠겨 있었다. **Step 2 를 마치면 `git diff main..HEAD` 를 걸어 표에 없는 변경 자리를 찾고 표를 넓힌다.** 그것이 이 Task 의 본체다.

- [ ] **Step 4: 워킹 트리가 깨끗한지 확인한다**

Run: `git status --short && git diff --stat`
Expected: 둘 다 비어 있다(미추적 문서 2개 제외).

- [ ] **Step 5: 확정 경로가 안 변했는지 실행으로 확인한다**

Run: `PYTHONIOENCODING=utf-8 python main.py > /tmp/after.txt 2>&1; echo "EXIT=$?"; grep -c "잔차" /tmp/after.txt`
Expected: EXIT=0, `잔차` 0건. 더미 데이터는 1단이 통과하므로 판정이 `confirmed` 이고 이 브랜치의 어떤 분기도 안 탄다.

`main` 워크트리와 **바이트 대조**한다:
```bash
git worktree add /tmp/base-64da0a9 64da0a9
cp -r data /tmp/base-64da0a9/data
(cd /tmp/base-64da0a9 && PYTHONIOENCODING=utf-8 python main.py > /tmp/before.txt 2>&1)
diff /tmp/before.txt /tmp/after.txt && echo "BYTE-IDENTICAL"
git worktree remove --force /tmp/base-64da0a9
```
Expected: `BYTE-IDENTICAL`. 다르면 확정 경로를 건드린 것이므로 **멈추고 보고한다**
(설계 §10 기준 6). `data/` 가 git 에 없으면 복사 대신 실제 경로를 확인해 맞춘다.

- [ ] **Step 6: 최종 스위트**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q --tb=short 2>&1 | tail -5`
Expected: 전부 통과. 최종 개수를 기록한다(새 기준선이 된다).

- [ ] **Step 7: 훼손 결과를 커밋한다 (테스트를 추가했다면)**

```bash
git add tests/
git commit -m "test(gate): 훼손 실험에서 안 잡힌 자리를 잠근다"
```

---

## 완료 후

1. `superpowers:requesting-code-review` 로 브랜치 전체 리뷰를 받는다(Opus 5). 리뷰어에게 **"규칙을 가르면 그 규칙의 소비자를 전부 찾았는가"** 를 특히 보게 한다 - 이 브랜치는 "반려된다" 를 단언하던 문장을 두 곳 고쳤고, 그런 문장이 더 있을 수 있다(`hypotheses.yaml` 4가설에 통계 해석 문단이 복붙돼 있다).
2. 지적을 반영한 뒤 `superpowers:finishing-a-development-branch` 로 main 에 ff 병합한다.
3. 메모리(`residual-output-merged.md` 옆에 새 파일)를 갱신한다: 새 기준선 커밋·테스트 수, 확정 계약, 남는 잔여(환각 반복 시 여전히 소각), ⏭ 다음 = **D/E(어휘·서술)**.
